# KORTEXOS™ — DECISION LOG

**Arquivo:** `docs/architecture/governance/KORTEXOS_5_1_2_DECISION_LOG.md`
**Produto:** KortexOS™
**Natureza:** registro histórico de decisões (padrão ADR — Architecture Decision Record). Append-only: decisões aprovadas NUNCA são editadas ou apagadas — apenas marcadas com status (Accepted / Superseded by DEC-XX / Refinada por DEC-XX). O registro superado permanece como prova de como o pensamento evoluiu.
**Companion doc:** `KORTEXOS_5_1_2_MASTER_BRIEFING_CANONICO.md` — a regra VIGENTE de cada decisão está especificada lá, sempre citando o(s) DEC(s)/D(s) que a fundamenta(m). Este arquivo é a prova/auditoria; o Master Briefing é a fonte de verdade operacional.
**Motivo da separação (2026-07-19):** o Master Briefing havia acumulado três tabelas RAGOV, duas tabelas de Veredito Red Team e uma tabela inteira ("Decisões pendentes") em que 8 de 9 itens já haviam sido decididos alhures — risco real de leitura desatualizada. Esta separação segue a prática consolidada de ADR (Microsoft, AWS, indústria): nunca apagar decisão superada, mas tirá-la do caminho de quem só precisa saber a regra atual.
**Como usar:** para saber a regra vigente de um tema, use o Master Briefing. Para entender POR QUE a regra é essa, ou o que ela substituiu, busque o ID (DEC-XX ou D-XX) aqui.

## Matriz cruzada DEC ↔ ADR

Esta matriz é um índice de rastreabilidade, não uma nova decisão. Ela registra apenas vínculos explícitos em documentos existentes; a ausência de vínculo não autoriza inferir aprovação ou supersessão.

| DEC | ADR relacionado | Papel do vínculo |
|---|---|---|
| DEC-01 / DEC-02 | [ADR 0006](../adr/0006-gorjeta-fora-da-comissao-e-motivo-do-estorno.md) | Formaliza a regra de gorjeta fora da comissão e sua aplicação operacional. |
| DEC-31 / DEC-32 | [ADR 0016](../adr/0016-onda0-units-architecture.md) | Aprovação do desenho de Units e autorização da Etapa 8 local da Onda 0. |
| DEC-34 | [ADR 0017](../adr/0017-onda1-payment-core-arquitetura.md) | Aprovação da arquitetura original da Onda 1. |
| DEC-38 | [ADR 0018](../adr/0018-onda1-integridade-financeira-imutavel.md) | Regularização da Etapa 8 corretiva e arquitetura forward-only de integridade financeira. |
| DEC-41 | [ADR 0019](../adr/0019-onda2-kortexflow-ledger-fundacao.md) | Aprovação do Blueprint da fundação KortexFlow da Onda 2. |
| DEC-42 | [ADR 0019](../adr/0019-onda2-kortexflow-ledger-fundacao.md) | Regulariza a Etapa 8 local e o hardening do ledger sem alterar o desenho de produto do ADR. |
| DEC-43 | [Protocolo de Automação Documental](KORTEXOS_DOCUMENTATION_AUTOMATION_PROTOCOL.md) | Governança documental de processo; não altera arquitetura de produto. |
| DEC-44 | — | Protocolo de otimização de código (Dark Launching via Feature Flags, Pre-flight Checks SQL, TDD e Git commit) — regra de processo registrada em `AGENTS.md`, sem ADR dedicado; não é a mesma decisão da Onda 3 (ver DEC-46). Correção: vínculo anterior apontava incorretamente para a ADR 0020, que é da Onda 3, não deste protocolo. |
| DEC-45 | [ADR 0021](../adr/0021-frontend-ux-responsiveness-and-adaptive-modals.md) | Refatoração de UI/UX, Modais Adaptativos sm/md/lg/xl, Bottom-Sheet Mobile, WCAG AAA e Governança IA. |
| DEC-46 | [ADR 0020](../adr/0020-onda3-compensation-staff-levels-sale-commission.md) | Aprovação do Blueprint da Onda 3 (Compensation) — staff levels e comissão de venda, fundação sem ativação. |
| DEC-47 | [ADR 0020](../adr/0020-onda3-compensation-staff-levels-sale-commission.md) | Regulariza a Etapa 8 (SQL local) e a promoção a `staging` das fatias 017-020 da Onda 3 — mesmo efeito de DEC-38/DEC-42, estendido para cobrir o ambiente. |
| DEC-48 | [ADR 0020](../adr/0020-onda3-compensation-staff-levels-sale-commission.md) | Emenda a fronteira "`checkout_close`/`create_appointment` intocados" (§1/§3.2 do Blueprint) para autorizar a fatia 021 — vínculo `order_items.package_id`. |
| DEC-49 | [ADR 0022](../adr/0022-onda4-calendar-policy-availability-resource-orchestration.md) | Aprovação do Blueprint da Onda 4 (Calendar Policy, Availability Resolver, Resource Orchestration) — fundação sem ativação. |
| DEC-50 | [DEC-50](../../../AGENTS.md) | Hardening dos agentes KORTEX e hooks determinísticos; decisão de tooling e governança, não de domínio de produto. |
| DEC-51 | [ADR 0023](../adr/0023-onda5-recurring-group-booking-waitlist.md) | Aprovação do Blueprint da Onda 5 (Recurring, Group Booking & Waitlist), com benchmark Booksy → players → cross-industry, waitlist sem hold persistente, grupo pai com appointments filhos e recorrência parcialmente materializada. |
| DEC-52 | [ADR 0023](../adr/0023-onda5-recurring-group-booking-waitlist.md) | Autoriza a Etapa 8 local da Onda 5, por fatia e via TDD; não autoriza `staging`, `main`, produção ou ativação. |
| DEC-03 a DEC-30, DEC-33, DEC-35 a DEC-37, DEC-39 e DEC-40 | — | Não há ADR dedicado ou vínculo explícito registrado; consultar a decisão primária neste log. |
## Índice de status

| Prefixo | Escopo | Numeração |
|---|---|---|
| DEC-01 a DEC-19 | Decisões formais do Platform Owner, registradas em 2026-07-19, escopo: comanda, gorjeta, comissão, pacotes, planos, cadastros, configuração, dados sensíveis | Cronológica, imutável |
| DEC-20 a DEC-52 | Decisões de processo/auditoria da ordem de construção, incluindo aprovação de Blueprints, regularizações de Etapa 8, governança documental, hardening de agentes e aprovação do Blueprint e autorização local da Etapa 8 da Onda 5 (DEC-51/DEC-52). | Cronológica, imutável |
| D-01 a D-09 | Decisões conceituais originais do anexo A1 (cadastros) — **todas as 9 resolvidas**, a última (D-07) por DEC-19 | Letrada, anterior às DEC |

---

# SEÇÃO 1 — REGISTRO DEC-01 A DEC-18 (Platform Owner, 2026-07-19)

Decisões tomadas em 2026-07-19, vinculantes a partir da aprovação deste anexo:

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-01 | Absorção de taxa da gorjeta | Gorjeta segue o MESMO modelo de absorção de taxa da forma de pagamento (Bruto Salão / Dividido / Bruto Staff), conforme a forma usada. Realidade operacional do salão desde o primeiro dia | Revoga a recomendação D-02 do anexo A1 |
| DEC-02 | Split de gorjeta multi-profissional | Comanda fechada com diversos serviços, produtos e múltiplos profissionais: rateio dinâmico ponderado por valor do serviço e tempo de execução (seção 5) | Nova regra canônica |
| DEC-03 | Reabertura de comanda | Além do estorno, existe a modalidade "reabrir comanda": após fechar, ao perceber erro, reabrir, editar e refechar — com preservação total do ledger (seção 4) | Nova regra canônica |
| DEC-04 | Preço por nível de profissional | SIM, no produto final. Override triplo por profissional×serviço: preço, tempo e valor (comissão) (seção 6) | Revoga a recomendação D-01b do anexo A1 |
| DEC-05 | Escopo | Especificação para produto final, não MVP. Sequência de construção do 5.1.1 permanece vinculante | — |
| DEC-11 | Reabertura e edição plena de comanda | Realidade obrigatória (evidência: telas do AppBarber em produção no Salão Esperança). Comanda finalizada oferece "Reabrir" imediatamente, com edição plena de itens, valores, profissionais e pagamentos — dentro das travas financeiras da seção 4.3 | Corrige a leitura de benchmark da versão anterior deste anexo |
| DEC-12 | Modal de edição de item | Clicar em qualquer item da comanda abre modal com TODAS as alterações possíveis (seção 4.5): valor, quantidade, profissional, comissão, desconto, cortesia, benefício, exclusão — governadas por permissão e trilha, nunca silenciosas | Nova regra canônica |
| DEC-13 | Duas semânticas de valor editável | (a) No item: altera o VALOR COBRADO daquele item (reprecificação, afeta base de comissão). (b) Na tela da comanda: DIVERGÊNCIA entre valor devido e valor cobrado — a diferença vira crédito ou débito do cliente na wallet, sem tocar itens nem comissão (seção 4.5) | Substitui a regra anterior de rateio automático do total |
| DEC-14 | Breakage de pacote — sequência em 4 estágios, mecanismo e texto finalizados | **Aprovado em 2026-07-19, substitui DEC-07. Finalizado em 2026-07-19 (mecanismo + texto + regra de extensão).** (1) Compra exige aceite digital — clique simples + assinatura desenhada na tela, com registro de hash/timestamp — usando o texto canônico da seção 11.4 (Parte II); (2) uso normal dentro da validade contratada; (3) ao vencer, abre automaticamente janela de extensão: cadastro do pacote define motivos elegíveis (subconjunto de saúde / férias / outro) e prazo MÁXIMO permitido (7, 14, 21 ou 28 dias); no momento da concessão, pessoa autorizada seleciona motivo e prazo dentro desse limite, registrando autor e data (T6); saldo remanescente vira crédito de wallet visível ao cliente durante a extensão, com aviso ativo; (4) só ao fim da extensão sem uso, aplica-se breakage puro (reconhecimento de receita, encerra o passivo). Reembolso por desistência ANTECIPADA (dentro da validade) é regra distinta e configurável por pacote: `valor pago − (sessões consumidas × preço avulso da época)`, nunca negativo; forma de devolução (dinheiro/wallet/percentual) configurável. **Segue com VENDA BLOQUEADA até validação jurídica** do mecanismo de aceite (clique + assinatura na tela) para o valor/perfil de contrato do Salão Esperança | Aceito pelo Platform Owner. Combina opções B+A como estágio intermediário, C como estágio final; adiciona reembolso configurável, aceite automatizado e regra de extensão por motivo+prazo enumerado |
| DEC-15 | Comissão pela venda (nova categoria, distinta da comissão de uso) | **Aprovado em 2026-07-19. Condição de clawback REFINADA por DEC-18 em 2026-07-19 — ver abaixo.** Escopo imediato: PACOTES. Cria uma segunda comissão, independente da comissão de execução/uso já existente (DEC-10, Gate 14): a **comissão de venda** remunera quem processou a venda do pacote (o "vendedor"), que pode ou não ser quem executa as sessões. Regras: (a) campo próprio no cadastro do pacote — % ou valor fixo, com vigência; (b) reconhece no momento do pagamento da venda (consistente com a tese "comissão nasce de venda real", seção 8.1/8.3), NÃO no consumo de sessões; (c) reversão governada por DEC-18 (não mais proporcional a qualquer reembolso — ver DEC-18); (d) independente da comissão de execução: as duas coexistem sem se reduzirem mutuamente; mesma pessoa pode receber ambas se vender e executar. **Escopo futuro, registrado e NÃO implementado agora:** estender comissão de venda a PLANOS DE ASSINATURA — pendente porque plano tem cobrança recorrente (múltiplos "momentos de venda": adesão + cada renovação), o que exige decidir se a comissão de venda se aplica só na adesão, em toda renovação, ou com degrau — decisão explicitamente adiada. **A condição de clawback (item c) já está pré-decidida por DEC-18 e vale desde já para quando essa extensão for implementada** | Nova regra canônica; fecha o ponto cego identificado no benchmark AppBarber (seção 8.2) |
| DEC-16 | Gorjeta e desconto — duas modalidades obrigatórias ($ ou %) | **Aprovado em 2026-07-19.** Todo campo que captura gorjeta ou desconto — no item (modal, seção 4.5), no combo (modo de preço, seção 9.2), no checkout (chips +desconto/+gorjeta, seção 8.4) e em qualquer outra superfície futura — oferece duas modalidades de entrada, nunca uma só: **valor fixo (R$)** ou **percentual (%)**. Quem lança escolhe a modalidade; o backend resolve e persiste sempre o valor absoluto (`_cents`) aplicado — a modalidade é UX de entrada, nunca ambiguidade armazenada (consistente com T9, frontend não deriva regra). Percentual de desconto/gorjeta calcula sobre a base resolvida do item ou da comanda, conforme o nível em que foi lançado | Nova regra transversal; corrige campos que hoje descrevem só uma modalidade (combo, item) |
| DEC-17 | Multa de desistência de plano de assinatura | **Aprovado em 2026-07-19. SUPERADA por DEC-18 em 2026-07-19** — o percentual de multa autônomo é removido; o problema que a multa tentava resolver (custos reais já incorridos) passa a ser resolvido pela fórmula unificada de reembolso (parte não utilizada) combinada com a regra de clawback condicionado a uso zero. Mantida aqui apenas como histórico da formulação original | Substituída por regra unificada (DEC-18) a pedido do Platform Owner |
| DEC-18 | Regra unificada de reembolso e comissão de venda — planos e pacotes | **Aprovado em 2026-07-19. Unifica e substitui a lógica de reembolso de DEC-15 (clawback proporcional) e DEC-17 (multa percentual).** Quatro princípios, válidos igualmente para planos e pacotes: **(1)** Comissão de EXECUÇÃO/USO, uma vez que a sessão/benefício foi efetivamente realizado, é definitiva e NUNCA entra em cálculo de devolução — está protegida por construção, porque a base de reembolso é sempre "parte não utilizada", nunca o total pago. **(2)** Fórmula de reembolso única: `valor pago − (valor da parte já utilizada, pelo preço avulso/preço de referência vigente à época)`, nunca negativo — mesma fórmula de pacotes (11.3), agora também para planos, reaproveitando o campo "preço de referência de consumo por item" que planos já possuem (seção 10.2) como base de cálculo. **(3)** Comissão de VENDA: clawback (reversão total) SE E SOMENTE SE o cliente não utilizou NENHUMA sessão/benefício do plano ou pacote; havendo qualquer uso, a comissão de venda é definitiva e não é revertida — mesmo que o restante não utilizado seja reembolsado. **(4)** Direito de arrependimento (art. 49 CDC): aplica-se somente quando a venda ocorreu FORA do estabelecimento comercial (online, app, telefone, KortexLink) — não cobre, pela leitura corrente da lei, venda presencial na comanda do salão. Dentro dos 7 dias corridos a contar da assinatura/aceite ou do recebimento, e sem qualquer uso, o reembolso é integral e imediato, sem dedução de qualquer custo — mandato legal, não política de negócio; nesse cenário o clawback total do item (3) também se aplica, pois uso=0. Fora desse prazo, ou em venda presencial, vale a política contratual dos itens (1)-(3), ainda sujeita a validação jurídica (ver ressalva) | Aceito pelo Platform Owner: "a comissão que já foi paga por serviço devidamente realizado não deve entrar na devolução"; modelo de pacotes migra para a fórmula de planos (parte não utilizada); clawback restrito a uso zero |
| DEC-19 | Resolução de D-07 — base legal de dados sensíveis do cliente | **Aprovado em 2026-07-19.** Fecha a última decisão conceitual em aberto do anexo A1. Base legal única: consentimento específico e destacado (LGPD art. 11, I) — verificado que não há alternativa viável, pois não existe "legítimo interesse" para dado sensível (art. 7º, IX, exclusivo de dado comum) e a exceção de "tutela da saúde" (art. 11, II, f) vale só para profissional/serviço/autoridade de saúde, o que exclui salão de beleza e barbearia. Módulo isolado especificado (Parte II §13, domínio D26): consentimento separado de qualquer outro aceite, com finalidade própria e destacada; dado sensível estruturado por serviço (não ficha médica ampla — minimização); visibilidade restrita a quem executa o atendimento + dono/gerente; EXCEÇÃO à regra T3 (arquivar sempre) — dado sensível tem prazo de retenção definido e é deletado/anonimizado de fato ao expirar ou por revogação, não arquivado para sempre; cliente pode consultar/excluir a qualquer momento (LGPD art. 18). **Texto exato do consentimento e prazo de retenção seguem como VALIDAÇÃO JURÍDICA PENDENTE** — mesma ressalva já aplicada a DEC-14/DEC-18; a base legal em si (art. 11, I) está fundamentada no texto da lei, a redação final não | Resolve D-07 (Seção 2 abaixo), que era a única das 9 decisões originais do anexo A1 ainda sem resposta |
| DEC-20 | Escopo e formato do Global Benchmark Map formal (etapa 3) | **Aprovado em 2026-07-19.** Dos 16 módulos obrigatórios do §23.2, priorizar os 6 que já estão em construção ativa (HOPE OS): 01 Booking & Capacity Scheduling, 02 Smart Gap/Waitlist/Resource Locks, 03 Checkout & Payments, 04 Ledger/Wallet/Current Accounts, 05 Compensation/Tips/Payout, 06 No-show/Deposit/Card-on-file. Os outros 10 módulos (07 a 16) entram na fila quando o subsistema correspondente virar prioridade real — não pesquisados agora, para não violar "básico antes do sofisticado" pesquisando Marketplace/White-label ou Experimentation/Incrementality antes de existir ledger. **Formato de execução:** iterativo — 2 a 3 módulos por rodada, dentro de conversas de chat, com matriz módulo × concorrente × achado × fonte apresentada a cada rodada para validação do Platform Owner antes de avançar (não via Research automatizado de uma vez). Primeira rodada: módulos 01 e 02 | Rejeitada a Opção A (benchmark completo dos 16 módulos de uma vez) e a Opção C (via função Research); escolhida Opção B (escopo) executada no formato D (iterativo) |
| DEC-21 | Estratégia canônica de benchmark: organização por domínio de excelência (Tier), não por concorrente | **Aprovado em 2026-07-19, redigido pelo Platform Owner.** Reformula a metodologia do Global Benchmark Map: nenhuma plataforma é modelo integral a reproduzir — cada referência vale para um domínio de excelência específico. Três tiers: **Tier 1 — Beauty Tech** (Booksy, Zenoti, Boulevard, Trinks, CashBarber, AppBarber, cada um com domínio de excelência declarado). **Tier 2 — Cross-Industry** (Uber, Aviação Comercial, Amazon, Apple, Disney — ainda não pesquisado, entra por módulo quando relevante). **Tier 3 — Domínios Técnicos/Verticais** (Fintech/ledger, Scheduling/yield acadêmico, Corporate wellness, Causal decisioning, Lifecycle automation, Messaging/AI — preserva as categorias do §23.1 original, que os Tiers 1/2 não cobrem). Mais **Referência Metodológica Permanente**: Toyota Production System (Lean/Kaizen) — framework de validação contínua, não benchmark competitivo. **Ressalvas de calibração adicionadas por Claude, aprovadas por decorrência da aprovação geral:** Trinks (parcialmente anti-padrão — edição retroativa de fechamento, já rejeitada), AppBarber (white-label BLOQUEADO no estágio atual — referência diferida, não imediata), CashBarber (lacuna de comissão de assinatura é o que o KortexOS™ supera, não copia), Booksy (conceito de marketplace pay-per-performance válido; mecanismo de atribuição do Boost tem risco documentado — não copiar sem regra de origem rastreável). §23.1 do Master Briefing passa a apontar para esta estratégia em vez de manter lista de referências duplicada | Substitui a lista de referências simples do §23.1 original; localização física: `KORTEXOS_5_1_2_GLOBAL_BENCHMARK_MAP.md`, seção "Estratégia Canônica de Benchmark" |
| DEC-22 | Comparative Proposal (etapa 4) aprovada — 42 achados classificados | **Aprovado em 2026-07-19.** Classifica os achados do Global Benchmark Map (módulos 01-06) em HERDAR/RENOMEAR/REFORÇAR/BACKLOG/BLOQUEAR/DESCARTAR (§23.3). Distribuição final: 15 HERDAR, 16 REFORÇAR, 10 BACKLOG, 1 DESCARTAR, 0 RENOMEAR, 0 BLOQUEAR. **Correções e adições do Platform Owner sobre a proposta original de Claude:** (1) 01.10 corrigido de DESCARTAR para REFORÇAR — agendamento por turma/grupo é válido para eventos reais (padrinhos, noivas), não modelo de turma fitness; (2) NOVO 01.13 — Agendamento recorrente (REFORÇAR CRÍTICO), gap real não coberto por nenhum domínio, validado por Vagaro e Boulevard; (3) NOVO 01.14 — Cancelamento recorrente, ocorrência vs. série inteira (REFORÇAR CRÍTICO), mesma dupla fonte; (4) NOVO 02.9a — Confirmação manual de pagamento offline, com/sem aviso ao cliente (REFORÇAR), validado por Wix Bookings e Salon Booking System; (5) NOVO 02.9b — **Pix Automático** do Banco Central, lançado jun/2025, obrigatório desde out/2025 (REFORÇAR CRÍTICO) — mapeia direto para o Subscription Engine (D18); (6) 06.3 resolvido — solução definida pelo Platform Owner: hold local/manual + retentativa automática ou lembrete próximo da data, validada por referência cruzada com pré-autorização hoteleira. Os outros 37 itens da proposta original de Claude foram aprovados sem alteração | Fecha a etapa 4 da ordem de construção (§22.1); registro completo em `KORTEXOS_5_1_2_COMPARATIVE_PROPOSAL.md`. Os 4 itens CRÍTICOS entram com prioridade alta na etapa 5 (Truth Map) |
| DEC-23 | Truth Map (etapa 5) aprovado — classificação de realidade técnica dos módulos 01–06 | **Aprovado pelo Platform Owner em 2026-07-20.** Auditoria de código real (não documental) contra o Master Briefing 5.1.2, escopo módulos 01–06 (DEC-20), com prioridade aos 4 itens REFORÇAR CRÍTICO de DEC-22. Achado: a fundação MVP (tenant/JWT/RLS em 17 tabelas, `create_appointment`/`update_appointment` idempotentes com concorrência otimista, `checkout_close` atômico, comissão em cascata profissional>serviço>grupo, PWA modular com sync incremental) é `REAL` e é a base a preservar, não a redesenhar. Confirmada `AUSENTE` por completo: Availability Resolver/Smart Gap/recursos físicos com lock/waitlist (D07/D11); agendamento e cancelamento recorrente (D09 — os 2 itens REFORÇAR CRÍTICO 01.13/01.14 de DEC-22); borda real de pagamento — PSP/Pix Automático/card-on-file/hold de depósito (D13 — os 2 itens REFORÇAR CRÍTICO 02.9b/06.3 de DEC-22, enum de forma de pagamento hoje é só rótulo local sem integração); KortexFlow — ledger double-entry append-only/wallet/staff current account/payout (D15/D16, hoje só existe `cash_entries` como caixa operacional). Também achado, fora do escopo dos 6 módulos: release não gateado pela CI (`deploy-pages.yml` publica independente de teste/lint) e configuração da segunda superfície de deploy (Render `kortex-pwa`) incompatível com `base` fixo do Vite e sem CORS liberado. **Veredito registrado: NO-GO para promoção direta ao produto final; GO apenas para iniciar a etapa 6 (Migration Map), começando pelos blocos 1–3 da ordem única de correção do Truth Map (fundação/release → borda de pagamento → KortexFlow mínimo)** | Fecha a etapa 5 da ordem de construção (§22.1) — muda o status de "Truth Map 5.1.1 — PENDENTE" para CONCLUÍDO; registro completo em `KORTEXOS_5_1_2_TRUTH_MAP.md` (v1.0). Desbloqueia o início da etapa 6 (Migration Map), que segue BLOQUEADA para SQL/schema até o Blueprint (etapa 7) ser aprovado (§22.2) |
| DEC-24 | Migration Map (etapa 6) aprovado — domínios, objetos e dependências mapeados | **Aprovado pelo Platform Owner em 2026-07-20.** Mapeia, para cada lacuna `CRÍTICO`/`AUSENTE` do Truth Map (DEC-23), domínio dono (D00–D31), nome de objeto proposto, estende/novo, dependência e impacto de promoção — sem SQL, sem coluna, sem tipo. 6 ondas de dependência: (1) Payment Core — `payment_intents`, `psp_webhook_events`, `card_on_file_tokens`, `deposit_holds`, `pix_automatico_mandates` (D13); (2) KortexFlow Ledger + Wallet — `kortex_accounts`, `kortex_ledger_transactions`, `kortex_ledger_entries`, `kortex_account_balances`, `client_wallets`, `staff_current_accounts`, `benefit_obligations`, `payout_batches` (D15/D16); (3) Compensation & Payout — `private.resolve_commission()` permanece intocada, `private.resolve_sale_commission()` + `commission_sale_records` novos e independentes (D17); (4) Calendar Policy + Availability/Resource — `calendar_policies`, `professional_shifts` (D02, achado suplementar fora do escopo original do Truth Map), `resources`, `resource_locks`, Availability Resolver sem cache na v1 (D07/D11); (5) Recurring/Group/Waitlist — `appointment_series` (estende `appointments`), `appointment_participants`, `waitlist_entries` (D09/D10/D07-D08); (6) versionamento de comanda — objeto de revisão de venda, altera fluxo de `checkout_close`/`order_refund`, só depois do ledger existir (D12/D15). **4 decisões de alto impacto resolvidas nesta aprovação:** (1) `payments` (MVP) e `payment_intents` (novo) coexistem permanentemente — captura via PSP gera linha em `payments`, nunca a substitui; (2) `resolve_commission()` (execução) fica intocada por ser `security definer` chamada 2× dentro da transação atômica de `checkout_close` e coberta por testes pgTAP existentes — comissão de venda (DEC-15) ganha função e tabela próprias; (3) sem cache de disponibilidade na v1 — Availability Resolver calcula on-demand, cache revisitado só se o Gate 03 mostrar necessidade com dado real; (4) schema de Payment Core e KortexFlow pode ser criado em paralelo, mas ativação de captura real de pagamento fica gateada até o ledger existir e o Gate 11 (Ledger Balance) passar — evita "saldo paralelo" (§20.6 do Master). **Achado suplementar registrado, não resolvido:** D02 (Calendar Policy & Availability Layer) não tem nenhuma tabela no schema MVP hoje e não fazia parte do escopo original do Truth Map (só módulos 01–06); recomenda-se auditoria dedicada, com o mesmo rigor de evidência, antes do Blueprint tratar Availability como pronta para desenho técnico | Fecha a etapa 6 da ordem de construção (§22.1); registro completo em `KORTEXOS_5_1_2_MIGRATION_MAP.md` (v1.0). Desbloqueia o início da etapa 7 (Blueprint), que segue exigindo aprovação própria antes do SQL Master (etapa 8, §22.2) |
| DEC-25 | Adendo do Truth Map aprovado — cobertura de D02 (Calendar Policy & Availability Layer) | **Aprovado pelo Platform Owner em 2026-07-20.** Fecha a lacuna de cobertura identificada pelo Migration Map (DEC-24, achado suplementar): D02 Calendar Policy & Availability Layer (Master Parte III §2) não fazia parte do escopo original do Truth Map (só módulos 01–06, DEC-20). Auditoria complementar confirmou `CRÍTICO`/`AUSENTE` em toda a extensão do domínio, com evidência de código: `organizations` (`supabase/migrations/20260712235319_mvp_baseline.sql:11-18`) e `professionals` (mesmo arquivo, linhas 125-136) não têm nenhuma coluna de horário, turno, timezone ou `settings`; nenhuma tabela de feriado, abertura/fechamento excepcional, folga ou bloqueio pontual existe em nenhuma das 12 migrations; `validateDateTime` (`backend/src/modules/appointments/appointments.validation.js:26-35`) só valida formato ISO 8601, sem checar expediente — confirmado que hoje é tecnicamente possível agendar em qualquer horário/dia, a única proteção real sendo o exclusion constraint GiST que impede sobreposição do mesmo profissional. Não altera o veredito geral do Truth Map (`NO-GO`, DEC-23) nem reabre DEC-23/DEC-24 — é cobertura complementar, não correção | Fecha a lacuna de cobertura de D02 antes do início da etapa 7 (Blueprint); registro completo em `KORTEXOS_5_1_2_TRUTH_MAP_ADENDO_D02.md` (v1.0). Confirma e eleva a prioridade da Onda 4 do Migration Map (`calendar_policies`, `professional_shifts`), sem criar onda nova |
| DEC-26 | Pontos Cegos Pré-Blueprint (Rodada 4) aprovados — 7 resoluções + 1 item de backlog | **Aprovado pelo Platform Owner em 2026-07-20.** Fecha a rodada de aprofundamento de camadas pedida explicitamente ("nova rodada de benchmark, levantamento de profundidade das camadas, procurar pontos cegos") — 4 frentes de pesquisa externa com fonte real (RODADA 4, `KORTEXOS_5_1_2_GLOBAL_BENCHMARK_MAP.md`) mais 1 achado estrutural interno. Resoluções: **(1) Hierarquia Empresa→Unidade (D01/D02/D28) — SUPERADA por DEC-27 em 2026-07-20** — confirmava o padrão Vagaro/Trinks como decisão consciente de escopo: `organizations` continuaria sendo o de-facto "unidade" enquanto o negócio for single-location (Salão Esperança); `units` como tabela própria só entraria quando D28 virasse prioridade real. Preservada aqui apenas como histórico da primeira decisão — o Platform Owner reconsiderou no mesmo dia e optou pelo padrão Zenoti/Mindbody (unidade como entidade própria desde já); ver DEC-27. **(2) Precedência de exceção de calendário simplificada** — "abertura excepcional" e "fechamento excepcional" (§2.3 do Master) deixam de ser dois níveis hierárquicos fixos; Blueprint implementa como um único objeto "exceção pontual" com campo de tipo (aberto/fechado/horário customizado), resolvido por recência de edição dentro do mesmo nível — mesmo resultado de negócio, mecanismo já provado em produção (padrão Dynamics 365 Field Service). **(3) Turno permite múltiplos blocos por dia** — `professional_shifts` (Onda 4, Migration Map) confirmado para suportar múltiplos registros por profissional/dia (horário partido/pausa) desde o desenho inicial, não um único par início/fim. **(4) Action Request com dois modos** — mantido o desenho assíncrono do Master como fundação (validado por precedente real em produção, Fineract/Mifos X), mas explicitamente também suporta colapso síncrono (draft→approved numa interação só, motivo obrigatório) quando o aprovador está presente e tem a permissão; ciclo assíncrono completo reservado para quando não há aprovador presente. **(5) Client Reliability Score — variável de antecedência** — "antecedência do agendamento no momento da marcação" (lead time) adicionada como 13ª variável considerada pelo score (§11.3 do Master), preditor forte de no-show na literatura, ausente da lista original. **(6) Client Reliability Score — eventos que pulam degrau** — evento classificado `CRÍTICO` (chargeback confirmado, fraude comprovada) pode saltar diretamente ao degrau máximo da régua progressiva, independente da contagem de ocorrências prévias. **(7) Client Reliability Score — escopo confirmado por organização** — sem compartilhamento cross-tenant, consistente com a postura de privacidade de DEC-19/D26; registrado como decisão consciente, não omissão. **Item de backlog aprovado, não urgente:** D00 Platform Owner Layer entra na fila nomeada de domínios futuros do Migration Map — nunca auditado até agora, zero módulo no código; acesso direto a banco/infra continua viável enquanto o KortexOS operar single-tenant real | Fecha a rodada de aprofundamento pré-Blueprint; registro completo em `KORTEXOS_5_1_2_PONTOS_CEGOS_PRE_BLUEPRINT.md` (v1.0) e `KORTEXOS_5_1_2_GLOBAL_BENCHMARK_MAP.md` (RODADA 4). Não reabre DEC-23/24/25. Item (1) confirma a Onda 4 do Migration Map (DEC-24) sem alterar suas linhas; itens (2)-(7) são refinamento/adição de detalhe para o Blueprint (etapa 7), que segue desbloqueado e passa a incorporar este material |
| DEC-27 | Migration Map — Onda 0 adicionada: `units` como entidade própria (D01), supera DEC-26 item (1) | **Aprovado pelo Platform Owner em 2026-07-20.** Reconsidera a decisão de escopo de DEC-26 item (1): em vez de tratar `organizations` como unidade de-facto (padrão Vagaro/Trinks), o KortexOS adota o padrão Zenoti/Mindbody — `units` vira entidade própria desde a Onda 4 do Migration Map, mesmo com o negócio hoje sendo single-location (Salão Esperança). Motivação: opção mais correta a longo prazo, evita retrofit doloroso depois que múltiplas tabelas já dependerem de `organization_id` como se fosse unidade. D01 (Identity & Tenant) já é `REAL` no Truth Map (DEC-23) — isso é extensão da fundação existente, não domínio novo do zero. Nova **Onda 0** no Migration Map, antes da Onda 1: `units` (novo, depende de `organizations`); vínculo profissional↔unidade (cardinalidade N:1 ou N:N não decidida — Blueprint resolve, o benchmark da Rodada 4 mostra os dois padrões em uso no mercado); escopo de `memberships` por unidade (aberto — nem o Master nem o benchmark resolveram isso publicamente para nenhum player). Onda 4 atualizada: `calendar_policies`/`professional_shifts` passam a depender de `units`, não mais de `organizations` diretamente — timezone e horário são propriedade da unidade (Master §2.4), agora modelado no nível certo desde o início. **Explicitamente NÃO decidido nesta DEC:** se as outras 5 ondas do Migration Map (Payment Core, KortexFlow, Compensation, Recurring/Group/Waitlist, versionamento de comanda) e as 19 tabelas MVP existentes (clients, professionals, services, appointments, orders etc.) também passam a escopar por `unit_id` — cada uma exige decisão própria no Blueprint; nenhuma mudou de dependência nesta DEC. Risco marcado `Alto`: é retrofit da fundação de tenant (D01), toca RLS e todo FK chain em produção — não é capability isolada como as demais ondas | Reabre e supera especificamente o item (1) de DEC-26 (não reabre os itens 2-7, o backlog D00, nem DEC-23/24/25). Atualiza `KORTEXOS_5_1_2_MIGRATION_MAP.md` para v1.1 (nova Onda 0 + Onda 4 revisada). Blueprint (etapa 7) segue desbloqueado, ainda não iniciado |
| DEC-28 | Decisões consolidadas: escopo de unidade finalizado (Migration Map v1.2), release/deploy, verificação operacional, higiene de repositório | **Aprovado pelo Platform Owner em 2026-07-20**, em resposta a um levantamento de todos os itens deixados explicitamente em aberto após DEC-27. **Escopo de unidade (finaliza a Onda 0 e classifica todas as demais ondas + 19 tabelas MVP existentes):** vínculo profissional↔unidade é **N:N** (`professional_units`, tabela de vínculo — não `unit_id` direto em `professionals`); `memberships` ganha **escopo híbrido** (`unit_id` nullable — papéis estruturais dono/gerente ficam org-wide com `unit_id` nulo; papéis operacionais profissional/recepção ficam por unidade); todas as Ondas 1-3/5-6 e as 19 tabelas MVP existentes classificadas em 3 categorias — **org-wide** (identidade/saldo da pessoa: `clients`, `professionals`, `client_wallets`, `staff_current_accounts`, `benefit_obligations`, `card_on_file_tokens`, `pix_automatico_mandates`, `staff_levels`), **catálogo/config com herança e override padrão Zenoti** (`services`, `products`, `service_groups`, `packages`, `professional_service_*`), ou **transacional com `unit_id` direto** (`appointments`, `orders`, `payments`, e todos os objetos novos de ledger/checkout/agenda/waitlist). Risco registrado para o Blueprint: wallets/contas correntes org-wide alimentados por lançamentos de ledger unit-scoped exigem reconstrução (Gate 13) que some múltiplas unidades. Detalhe completo em `KORTEXOS_5_1_2_MIGRATION_MAP.md` v1.2. **Release/deploy:** (a) corrigir o conflito `base` do Vite × domínio Render e completar `CORS_ORIGINS` — mantém as duas superfícies de deploy (GitHub Pages + Render) funcionando; (b) deploy do Pages passa a depender do sucesso da CI (hard gate); (c) criar ambiente de homologação via segundo projeto Supabase + Render gratuitos — **fechado por DEC-30 em 2026-07-22**. **Verificação operacional:** (d) confirmar paridade da migration Fase 11 em produção; (e) reexecução integral das 3 suítes de teste adiada para o início do Blueprint (baseline), não executada agora. **Higiene de repositório:** (f) remover a worktree `claude/gracious-northcutt-61f4e4` (conteúdo já mesclado em `main` via PRs #9/#10, confirmado seguro); (g) commitar o diff de `OrganizationContext.jsx`/`OrganizationContext.test.jsx` (trabalho do usuário, confirmado pronto). **Validação jurídica (DEC-14/18/19):** confirmado — o Blueprint desenha o schema normalmente em paralelo à validação jurídica; só a ativação comercial (venda de pacote/plano, coleta de dado sensível) continua bloqueada até o parecer sair, sem mudança da regra já registrada em DEC-14/18/19 | Não reabre DEC-23/24/25/26/27 — finaliza o que DEC-27 deixou explicitamente pendente e resolve itens operacionais independentes. Atualiza `KORTEXOS_5_1_2_MIGRATION_MAP.md` para v1.2. Execução das decisões (a)/(b)/(d)/(f)/(g) prossegue na mesma sessão (commits e mudanças de arquivo próprios, não novas DEC); (c) exigiu passos manuais do Platform Owner no painel do Render/Supabase, concluídos e fechados por DEC-30. Blueprint (etapa 7) segue desbloqueado, ainda não iniciado |
| DEC-29 | Encerramento formal da construção do MVP técnico (Trilhas A–E) e transição da execução para a Trilha F (5.1.2) | **Aprovado pelo Platform Owner em 2026-07-20.** Declara concluída a construção do MVP técnico (Fases 1–11, incl. Onda 6/7 do Design System) e encerra a numeração de "Fases 8+" de `PLANEJAMENTO_EXECUCAO_UNIFICADO.md` como fonte ativa de sequenciamento — **toda execução nova passa a seguir as 7 Ondas do `KORTEXOS_5_1_2_MIGRATION_MAP.md`, gateadas pelo Blueprint (etapa 7)**, não mais a numeração de Fase. Os temas que tinham Fase alocada e ainda não foram executados não são abandonados, apenas re-hospedados: Sessões de Caixa/Void (era Fase 12) e Comissões Escalonadas (era Fase 13) → Onda 3 (Compensation); No-show/Sinal (era Fase 14) → Master Briefing D24 (Trust & Retention) + Onda 1 (Payment Core); Calendário Operacional (era Fase 15) → Onda 4 (Calendar Policy/Availability), já coberta por `KORTEXOS_5_1_2_TRUTH_MAP_ADENDO_D02.md`; Agenda Transacional (era Fase 16) → Master Briefing D07-D09. **Reorganização documental executada na mesma sessão (não é DEC própria, é execução mecânica desta):** toda a documentação de planejamento/status do MVP (`KORTEX_MVP_TECNICO.md`, `PROJECT_STATE.md`, `PLANEJAMENTO_EXECUCAO_UNIFICADO.md`, `PLANEJAMENTO_FINANCEIRO.md`, `PLANEJAMENTO_COMISSOES.md`, `PWA_PLANEJAMENTO.md`, `PLANEJAMENTO_ROADMAP_POS_MVP.md`, `PLANEJAMENTO_CALENDARIO_OPERACIONAL.md`, `PLANEJAMENTO_AGENDA_TRANSACIONAL.md`, `FASE_10_PLANO.md`, `FASE_10_STATUS.md`, `REDTEAM_FASE7.md`, `P0_AUDITORIA_SERVICOS_PROFISSIONAIS.md`, `SERVICE_PROFESSIONAL_OPTION_C_AUDIT.md`, e o prompt de execução da Onda 6/7 renomeado de `instrução designer.md`) foi movida via `git mv` para `docs/legacy/mvp-tecnico/` (manifesto completo em `docs/legacy/mvp-tecnico/README.md`); os 3 documentos mais referenciados (`PROJECT_STATE.md`, `PLANEJAMENTO_EXECUCAO_UNIFICADO.md`, `KORTEX_MVP_TECNICO.md`) receberam banner de arquivamento no topo; `docs/INDEX.md` foi reescrito com a Trilha F como trilha ativa primária; `AGENTS.md` teve a Ordem de Leitura e a seção de visão pós-MVP atualizadas na mesma direção. Nada foi apagado em substância — só a nota `NOTA_2026-07-20_arquivo_nao_revisado.md`, cuja própria questão (origem do arquivo `instrução designer.md`) foi resolvida nesta sessão (era o prompt real da Onda 6/7, já executada) e cujo próprio texto autorizava a remoção após leitura. **Não reabre nem contradiz DEC-23/24/25/26/27/28** — é uma decisão de camada acima (encerra a trilha de execução antiga, não revisita nenhuma decisão técnica já tomada dentro dela) | Fecha formalmente as Trilhas A–E como trabalho concluído; consolida `docs/INDEX.md` e `AGENTS.md` em torno da Trilha F como única trilha de execução ativa a partir desta data |
| DEC-31 | Aprovação do Blueprint da Onda 0 (`units`) | **Aprovado pelo Platform Owner em 2026-07-22 (sessão pós-sessão anterior). Revisão post-hoc de design feita por Claude (não por Red Team original), resolvendo divergências entre o texto de 2026-07-21 (preso numa branch nunca mesclada + alegação de verificação não confirmada) e decisões técnicas de hoje.** Escopo: `units`, `professional_units`, escopo híbrido de `memberships`, `membership_permissions` (allowlist), `unit_access_audit_events` (append-only). Seis fatos MVP (`appointments`, `orders`, `order_items`, `payments`, `inventory_movements`, `cash_entries`) ganham `unit_id` nullable com default-fill via triggers, nunca reescrita das RPCs. Memberships e organizations recebem triggers `BEFORE INSERT` / `AFTER INSERT` de default-fill. Timezone fixa (`America/Sao_Paulo`, único tenant real é Salão Esperança — DEC-26 item 7). `create_organization` permanece inalterada (RPC desempenha o chamador). Backfill inline na migration (não script separado). — **Veredito registrado: QA Red Team `GO` de desenho; nenhuma implementação ainda autorizada até aprovação explícita de SQL (Etapa 8/DEC-32).** | Move a Onda 0 de "nunca mesclada, com alegação não confirmada" para "Blueprint aprovado, pronto para QA de implementação" |
| DEC-32 | Autorização da Etapa 8 (SQL) da Onda 0 para ambiente local | **Aprovado pelo Platform Owner em 2026-07-22, decorrência interna de DEC-31.** Após o `GO` de desenho do QA Red Team, autoriza criar e testar migration e implementação da Onda 0 exclusivamente em ambiente local (supabase db reset), com executáveis (pgTAP, advisors, testes backend/frontend de regressão). Não autoriza produção: deve passar o Red Team de implementação (fora desta sessão) antes de qualquer push a staging. | Desbloqueia a execução local da Onda 0; produção segue bloqueada até validação completa |
| DEC-33 | Reforma do processo MAS: fatiamento vertical, TDD por fatia, evidência bruta obrigatória e registro automático de decisão | **Aprovado pelo Platform Owner em 2026-07-24, motivado pela auditoria da própria Onda 0** (Blueprint desenhado e migration inteira escrita como um único incremento; a lacuna de RLS org-wide e FK `RESTRICT` só foi achada em auditoria pós-hoc, já com o incremento declarado "concluído" — ver correção forward-only registrada em `KORTEXOS_5_1_2_ONDA_0_IMPLEMENTATION.md`). Preenche `kortex-blueprint-architect` (Etapa 7), até então um stub vazio sem `SKILL.md` — cobre a lacuna com `$grill-me` como método de fechar decisão de desenho aberta antes de redigir. Insere onda de **Fatiamento** (`$prd-to-issues`, vertical slice/tracer bullet, `issues/NNN-titulo.md`, marcação `HITL`/`AFK`) entre Desenho e Implementação — nenhuma Onda inteira mais vira um único incremento não fatiado. Implementação passa a usar `$tdd` por fatia (teste antes do código, nunca "todos os testes primeiro, todo código depois"). `$kortex-qa-redteam` passa a exigir evidência bruta (comando executado, saída real) em vez de aceitar relato resumido de subagente delegado — embutindo estruturalmente a regra que antes só existia como lembrete (incidente de alegação de verificação fabricada, 2026-07-21/22). Registro de DEC/ADR de toda aprovação formal (Blueprint, Etapa 8, promoção de ambiente) vira passo de saída obrigatório da skill que obteve a aprovação, não mais tarefa separada dependente do Platform Owner lembrar de pedir. Avaliadas e conscientemente excluídas do fluxo de Onda: `$write-a-prd` (redundante com o Blueprint dentro do pipeline de Onda; uso legítimo só fora dele), `$find-skills` (meta-ferramenta de descoberta, não etapa de execução), `$better-sqlite3-rebuild` (projeto não usa SQLite). | Onda 1 (Payment Core) e todas as ondas seguintes do Migration Map passam a seguir o fluxo de 6 ondas (Descoberta → Desenho → Fatiamento → Implementação → Validação → Integração) definido em `.agents/skills/kortex-mvpt-orchestrator/references/mas-contracts.md`; nenhum gate de aprovação (Blueprint, SQL, promoção) muda de lugar ou de rigor |
| DEC-34 | Aprovação do Blueprint da Onda 1 (Payment Core) | **Aprovado pelo Platform Owner em 2026-07-25.** Primeiro Blueprint desenhado sob o processo reformado (DEC-33): 9 decisões fechadas via `$grill-me` — modelo de PSP único como plataforma/marketplace (padrão "Separate Charges and Transfers"), fornecedor real deferido para a Etapa 9 (schema agnóstico via coluna `provider`), outbox mínimo específico de webhook sem generalizar (resolve DEC-22 06.3), escopo reduzido excluindo `card_on_file_tokens`/`pix_automatico_mandates` (bloqueados por D26/D18, nenhum com Migration Map próprio), `payment_intents.order_id` nullable com reconciliação isolada em `checkout_close`, comissão de no-show configurável por serviço (confirmada como necessidade real de operação, não hipotética — colaborador desconfiaria se não visse a cobrança refletida na própria comissão). Liquidação de no-show resolvida como pedido sintético real (`order`/`order_item`/`payment`) via RPC nova e pequena, deliberadamente fora de `checkout_close`, para reaproveitar o único caminho de visibilidade de comissão que já existe hoje. Passou por 2 rodadas de `$kortex-qa-redteam`: 1ª `NO-GO` com 5 achados (sem dedup de webhook, sem unicidade de `payment_intents`, sem trava de concorrência entre captura por checkout e por no-show, autorização de RPC não especificada, overflow de reconciliação não endereçado) — todos fechados com mecanismo concreto (unicidade `(provider, provider_event_id)` e `(organization_id, provider, provider_reference)`, CAS `WHERE status = 'active'` mesma disciplina da ADR 0012, autorização herdada de `appointment`, estorno existente ADR 0006/0007 para o excedente, expiração via webhook reativo + fallback de captura falha sem scheduler novo); 2ª rodada `GO`. Ver ADR 0017 para o detalhamento arquitetural. | Libera o Fatiamento (`$prd-to-issues`) da Onda 1 como próximo passo; Etapa 8 (SQL) continua exigindo autorização própria do Platform Owner, não incluída nesta aprovação |
| DEC-35 | Implementação da Onda 1 (Payment Core) concluída e mesclada em `staging` | **Qualificado por DEC-36 (2026-07-26): Red Team pós-merge achou um bug crítico de cruzamento de depósito e outros 3 blockers — o veredito desta entrada de "concluída" vale para "implementação física em staging", não para "contrato completo pronto para produção"; ver DEC-36 para os blockers e o veredito correto (NO-GO).** Concluído em 2026-07-26, fecha o ciclo aberto por DEC-34 (Blueprint aprovado) sob o processo de DEC-33 (fatiamento + `$tdd` + evidência bruta obrigatória). As 5 fatias verticais do Fatiamento (`issues/001` a `issues/005`) foram implementadas em sequência de dependência, cada uma com teste antes do código: **001** — extensão aditiva de `services` com política de depósito/no-show (5 colunas nullable, nenhuma constraint existente alterada). **002** — schema de `payment_intents`/`psp_webhook_events` + outbox de ingestão de webhook idempotente (dedup via unicidade `(provider, provider_event_id)`; sem integração real de PSP, deferida para a Etapa 9). **003** — `deposit_holds` + RPC nova `deposit_hold_create` (chamada pelo backend logo após `create_appointment`, que permanece intocada), com snapshot da política do serviço no momento da criação (mesmo padrão da ADR 0011). **004** — marcada **HITL** pelo próprio Blueprint (maior risco da onda): `checkout_close` ganha reconciliação de depósito via adição isolada — corpo da função é cópia integral da versão vigente (`supabase/migrations/20260715103200_fase9_foundation.sql`, não a referenciada originalmente no issue, já superada antes do início desta fatia) mais um bloco aditivo; CAS (`WHERE status = 'active'`) e overflow via estorno existente (ADR 0006/0007), nunca crédito novo. **005** — RPC nova `no_show_settlement_create`, deliberadamente isolada de `checkout_close`/`resolve_commission()`, mesmo CAS da fatia 004 (mutuamente exclusivas), comissão do snapshot em `deposit_holds`. Antes do merge, a suíte pgTAP completa foi reexecutada do zero a cada fatia (**434/434** ao final, incluindo os testes pré-existentes de `checkout_close` sem nenhuma regressão) e a suíte backend completa (**291/291**) — evidência bruta conferida pessoalmente, não relato resumido de subagente (disciplina de DEC-33). Merge para `staging` via PR #19 (`feat/onda1-fatiamento-issues` → `staging`), autorizado explicitamente pelo Platform Owner após os 3 checks obrigatórios de CI (`Security & Linting`, `Supabase & Backend Tests`, `Frontend Build & Test`) reportarem sucesso; merge commit `2b5dd086c780a5b1d47b3d71a889c681dda2c027`. **Achado registrado, não bloqueante:** a autorização das RPCs novas (003 e 005) implementa a regra **real** hoje aplicada por `create_appointment`/`update_appointment` — owner/admin/manager/reception, org-wide — e não o texto do Blueprint (§7.1), que descreve "reception/professional restritos à unidade"; nenhuma das duas RPCs existentes de agendamento aplica esse escopo por unidade hoje, então herdar a regra real (não a aspiracional) evita inventar superfície de autorização nova, mas o texto do Blueprint §7.1 fica desalinhado do comportamento implementado. **Não promovido a `main`/produção nesta decisão** — só `staging` | Fecha o ciclo de execução aberto por DEC-34. Não reabre DEC-33/34. Registra o achado de §7.1 para eventual correção de redação do Blueprint ou promoção real de escopo por unidade em onda futura; não altera nenhuma regra já implementada |
| DEC-36 | Red Team pós-merge da Onda 1 — NO-GO para `main`/produção, 1 bug crítico e 3 blockers adicionais | **Registrado em 2026-07-26**, motivado por teste adversarial do Platform Owner sobre o código já mesclado em `staging` (DEC-35). Cada achado foi conferido pessoalmente contra o código antes deste registro (disciplina de DEC-33 — evidência bruta, não relato aceito por confiança), não apenas transcrito do relatório recebido. **Achado crítico (bloqueia produção):** a reconciliação de depósito em `checkout_close` (`supabase/migrations/20260726170000_onda1_checkout_close_deposit_reconciliation.sql:384-390`) valida a captura do `deposit_hold` só por `organization_id` + `appointment_id` — nada liga o `appointment_id` informado no payload aos itens/cliente do checkout que está de fato sendo fechado. Um checkout com itens de qualquer natureza, desde que acompanhado do `appointment_id` de outro agendamento com hold ativo (mesmo de outro cliente, outro serviço), captura esse hold e aplica seu valor ao pedido não relacionado. Bug de dinheiro, confirmado em produção de código, não hipotético. **Blockers adicionais, cada um confirmado por leitura direta:** (1) os status `released`/`expired` de `deposit_holds` existem só no `CHECK` do enum (`20260726160000_onda1_deposit_holds_creation.sql:23`) — nenhuma RPC atribui esses valores; não há caminho de liberação de hold nem de expiração automática/reativa, apesar do texto do Blueprint §3.3 descrever ambos os gatilhos. (2) O outbox de webhook (`pspWebhookEvents.service.js:19-34`) não reprocessa dead-letter na prática: se o mesmo `provider_event_id` for reentregue depois que o `payment_intent` correspondente passar a existir, o `upsert(ignoreDuplicates)` já encontra a linha (mesma dedup-key do dead-letter original) e retorna `duplicate: true` **antes** de tentar resolver o vínculo — o comentário do código ("reprocessable once the intent shows up") descreve um comportamento que a implementação não tem. (3) Nunca houve um DEC autorizando explicitamente a Etapa 8 (SQL) da Onda 1, equivalente ao DEC-32 da Onda 0 — `docs/INDEX.md` e o Blueprint (`KORTEXOS_5_1_2_BLUEPRINT_ONDA_1_DRAFT.md`) continuam dizendo literalmente "Etapa 8 continua exigindo autorização própria, ainda não concedida", texto nunca atualizado apesar de todo o SQL das 5 fatias já ter sido escrito e mesclado — os pedidos diretos do Platform Owner ("implementar a fatia N") foram tratados como autorização ad-hoc suficiente, sem o passo de registro formal e próprio que o processo de DEC-33 prevê para Etapa 8. **Não são blockers, são escopo já conhecido e deliberadamente fora desta onda:** captura financeira real via PSP (provider ainda `internal`, Etapa 9 não autorizada) e captura financeira real ficando bloqueada até a Onda 2 (ledger) + Gate 11 existirem (`KORTEXOS_5_1_2_MIGRATION_MAP.md:64`) — nenhum dos dois é regressão desta sessão, ambos já registrados como fora de escopo desde DEC-34/§1 do Blueprint. **Evidência positiva confirmada, não apenas relatada:** `supabase db reset` limpo (20 migrations), pgTAP 434/434, backend 291/291, frontend 106/106, `get_advisors` sem achados, CI do commit `0aa5c3a` verde. **Veredito:** implementação física em `staging` é REAL (código existe, roda, os testes escritos passam); o contrato completo da Onda 1 é PARCIAL — os testes escritos não cobriam o cenário de cruzamento entre agendamentos, então "434/434 verde" não equivale a "sem bugs", só a "sem regressão no que foi pensado para testar"; promoção para `main`/produção é **NO-GO** até os 4 itens acima fecharem | Qualifica DEC-35 (não a invalida — a implementação e o merge aconteceram como descrito, o veredito de completude é que estava errado). Não reabre DEC-33/34. O bug crítico é corrigido na mesma sessão deste registro, com `$tdd`, e vira uma DEC própria de fechamento quando concluído; os outros 3 blockers permanecem em aberto, sem DEC de fechamento ainda |
| DEC-37 | Correção do bug crítico de cruzamento depósito↔agendamento↔comanda (DEC-36) | **Corrigido em 2026-07-26, mesma sessão do registro de DEC-36.** Nova migration (`20260726190000_onda1_checkout_close_deposit_appointment_guard.sql`) redefine `checkout_close` de novo (não edita a migration da fatia 004, já aplicada em `staging`) — corpo idêntico ao vigente, com uma guarda nova antes da tentativa de CAS: só reconcilia o `deposit_hold` do `appointment_id` informado se esse agendamento pertencer ao mesmo `client_id` do checkout **e** seu `service_id` estiver entre os itens de serviço já inseridos neste mesmo pedido; caso contrário o comportamento é idêntico a "nenhum `appointment_id` informado" (mesmo caminho de no-op já usado pelo CAS de corrida com a fatia 005) — nunca um erro novo, mas também nunca uma captura equivocada; se o chamador esperava o desconto do depósito e ele não se aplica, a checagem pré-existente `v_paid <> v_total` já rejeita o checkout com o erro genérico de sempre. **`$tdd` aplicado ao pé da letra:** teste pgTAP novo (`rpc_checkout_close_deposit_appointment_guard_test.sql`) escrito e executado ANTES da correção, reproduzindo o exploit exatamente como descrito em DEC-36 (checkout de cliente B por serviço não relacionado, carregando o `appointment_id` do cliente A) — confirmado `RED` (o hold do cliente A era capturado incorretamente) antes de qualquer mudança de código; a correção então o torna `GREEN`, junto de um caso de sanity check confirmando que um checkout legítimo (mesmo cliente, mesmo serviço) continua reconciliando normalmente. Os 3 testes pré-existentes que dependiam do comportamento antigo (`rpc_checkout_close_deposit_reconciliation_test.sql`, mais o teste de integração backend) tiveram seus payloads corrigidos para incluir `client_id` — não estava faltando propositalmente, só nunca tinha sido exercitado porque o código antigo nunca checava esse campo. **Evidência bruta, comando real, conferida pessoalmente:** `supabase db reset` limpo (23 migrations), pgTAP **438/438** (434 + 4 do teste novo de guarda), backend **292/292** (291 + 1 teste de integração novo do exploit via HTTP real), `npm run lint` sem erros. **Veredito atualizado:** o achado crítico de DEC-36 está fechado e verificado; os outros 3 blockers (released/expired sem transição, dead-letter de webhook não reprocessável, ausência de DEC de autorização da Etapa 8) continuam em aberto — promoção para `main`/produção **permanece NO-GO** até eles fecharem | Fecha o achado crítico de DEC-36 (não o invalida — o achado estava correto, esta é a correção). Não reabre DEC-33/34/35. Não altera o veredito geral de NO-GO da Onda 1 — só remove um dos 4 itens da lista de blockers |
| DEC-41 | Aprovação do Blueprint da Onda 2 (KortexFlow Ledger + Wallet & Current Accounts) | **Aprovado pelo Platform Owner em 2026-07-26.** Blueprint desenhado sob o processo de DEC-33: 11 decisões fechadas via `$grill-me` — escopo de fundação sem ativação (`checkout_close` intocado nesta Onda, ativação real fica pra uma Onda 2b futura); `kortex_account_balances` como projeção mantida só por trigger `AFTER INSERT` (nunca view recalculada, nunca escrita direta); `kortex_accounts` como o próprio razão auxiliar por unidade, com FK dupla nullable tenant-safe (`client_id`/`professional_id`) em vez de par polimórfico `owner_type`/`owner_id`; resolução do conflito entre `kortex_accounts` por unidade (Migration Map) e `client_wallets`/`staff_current_accounts` org-wide via projeção em cascata de dois níveis; formato de `kortex_ledger_entries` (`direction` + `amount_cents` sempre positivo, nunca valor assinado; `idempotency_key` no cabeçalho via `private.idempotency_keys` já existente; `unit_id` denormalizado); porta única de escrita (`kortex_ledger_post`, valida double-entry em `plpgsql`, sem `CONSTRAINT TRIGGER` deferido — sem precedente neste schema); RLS em 4 grupos (dado financeiro cru = owner/admin/manager só; `staff_current_accounts` com self-view do profissional, Gate 02; `client_wallets` também pra reception); taxonomia fechada de `kortex_accounts.kind` + seed antecipado das contas fixas via trigger em `units`; schema de `benefit_obligations`/`payout_batches` sem produtor (D18 não existe); backfill retroativo das contas fixas pras unidades já existentes em `staging`. Passou por 2 rodadas de `$kortex-qa-redteam`: 1ª `NO-GO` com 3 achados (criação de conta por entidade sem trava de concorrência, `kortex_ledger_post` sem validação de tenant por linha, autorização da RPC não especificada) — todos fechados com mecanismo concreto (`INSERT ... ON CONFLICT DO NOTHING` + `SELECT`, checagem explícita de `organization_id`/`unit_id` por linha antes de qualquer `INSERT`, `actor_has_role(owner/admin/manager)`); 2ª rodada `GO`. Ver ADR 0019 para o detalhamento arquitetural, incluindo as alternativas rejeitadas | Libera o Fatiamento (`$prd-to-issues`) da Onda 2 como próximo passo; Etapa 8 (SQL) continua exigindo autorização própria do Platform Owner, não incluída nesta aprovação. Não reabre DEC-24/27/28/33 |
| DEC-42 | Regularização da Etapa 8 (SQL) da Onda 2 e correção pós-auditoria do bypass de escrita por `service_role` no ledger | **Autorização dada pelo Platform Owner via instrução direta em 2026-07-27** ("autoriza a etapa 8, implementar a fatia 012 com $tdd") — tratada como autorização ad-hoc suficiente para iniciar a implementação, mas, como em DEC-38 (mesmo padrão para a Onda 1), sem o registro formal próprio que o processo de DEC-33 prevê para Etapa 8; regularizada agora. Escopo autorizado: projetar, criar e testar migrations das 5 fatias verticais da Onda 2 (`issues/012` a `issues/016`) exclusivamente em ambiente local (`supabase db reset`), com `$tdd` (ciclo red-green por critério de aceite) e evidência bruta própria (não relato de subagente — disciplina de DEC-33), em branch dedicada (`feat/onda2-fatia-012-kortex-accounts`, nunca commitada direto em `staging`). Não autoriza `staging` nem `main`/produção. As 5 fatias foram implementadas e verificadas: **012** `kortex_accounts` (plano de contas + razão auxiliar por unidade, trigger de seed + backfill retroativo); **013** `kortex_ledger_transactions`/`entries` + RPC `kortex_ledger_post` (double-entry, os 3 achados do Red Team de desenho exercitados por teste, incluindo uma lacuna real encontrada e corrigida na própria sessão — a validação de tenant do achado #2 cobria `organization_id` mas não `unit_id`); **014** `client_wallets`/`staff_current_accounts` (projeção nível 2, self-view do profissional); **015** `benefit_obligations` (schema puro); **016** `payout_batches`/`payout_batch_items` (schema puro). **Auditoria pós-implementação (2026-07-27):** o Platform Owner reportou veredito NO-GO com 5 achados; cada um foi conferido pessoalmente contra o código antes deste registro (disciplina de DEC-33), não aceito por relato — 2 achados eram reais (ausência deste registro de Etapa 8; `service_role` com `INSERT`/`UPDATE`/`DELETE` diretos nas 3 tabelas do ledger, contornando `kortex_ledger_post`), 1 estava contraditado pela evidência (suíte PWA reportada como 105/106 rodou **106/106** tanto isolada quanto completa, sem a falha citada), 1 estava fora de contexto (branch fora de `staging` era decisão conjunta já tomada na mesma sessão, não achado novo), e o achado de `service_role` estava corretamente identificado mas mal enquadrado como exclusivo da Onda 2 — o mesmo padrão de grants (`service_role` com DML completo por default da plataforma Supabase) existe em `deposit_holds`/`payment_intents`/`cash_entries`/`orders` desde o `mvp_baseline`, nunca fechado em nenhuma Onda anterior. **Decisão de escopo do Platform Owner:** fechar o bypass só no ledger da Onda 2 (não retroativamente no resto do schema) — `REVOKE INSERT, UPDATE, DELETE` de `service_role` em `kortex_ledger_transactions`/`kortex_ledger_entries`/`kortex_account_balances`, migration `20260727170000_onda2_kortex_ledger_service_role_write_lockdown.sql`, com teste de regressão que impersona `service_role` (`SET LOCAL role service_role`) e confirma: escrita direta rejeitada (`42501`), `kortex_ledger_post` continua funcionando (bypassa via privilégio do dono da função, `SECURITY DEFINER`, não do chamador). | Regulariza prospectivamente a Etapa 8 da Onda 2, mesmo efeito de DEC-38 para a Onda 1. Não invalida o histórico das fatias 012-016 nem autoriza promoção a `staging`/`main` — essa continua exigindo os gates de ambiente (Environment Guardian, Delivery Guardian) e homologação próprios. Estabelece o ledger financeiro double-entry como padrão de grant mais rígido que o resto do schema; não reabre nem estende essa exigência às tabelas de outras Ondas |
| DEC-30 | Ambiente de homologação (staging) criado e validado — fecha DEC-28(c) | **Concluído em 2026-07-22.** Modelo de 2 branches (`main` produção, `staging` homologação — sem `develop`). Segundo projeto Supabase gratuito (`kortex-os-v2-staging`, ref `hyzoocmmtmjlgdyifwhu`) com as 12 migrations aplicadas e verificadas por chamada própria (`list_migrations`/`list_tables`/`get_advisors`, não por relato de subagente — ver incidente de fabricação de relatório registrado em `kortex-supabase-guard`, seção 6). Dois serviços novos no Render (`kortex-api-staging`, `kortex-pwa-staging`), blocos aditivos em `render.yaml`, branch de deploy `staging`. CI (`ci.yml`) ampliada para rodar também em `staging`. Achado durante a validação: o primeiro deploy do `kortex-api-staging` falhou (`Exited with status 1`) por faltar `SITE_URL` — variável exigida em runtime por `backend/src/config/env.js` (redirect do convite de equipe por e-mail, ADR 0014) que também faltava no bloco de produção do `render.yaml` (produção só funciona porque o valor foi configurado manualmente no painel do Render, fora do que o arquivo documenta); corrigido nos dois blocos. Validação final direta (não por confiança): bundle JS publicado do `kortex-pwa-staging` inspecionado por download+grep, confirmando referência exclusiva ao Supabase de staging (zero ocorrência do ref de produção) e `VITE_API_BASE_URL` apontando para `kortex-api-staging.onrender.com`. Proteção de branch aplicada via `gh api`: `main` exige PR, 3 checks de CI obrigatórios (`Security & Linting`, `Supabase & Backend Tests`, `Frontend Build & Test`) e `enforce_admins: true` (sem exceção nem para o dono), sem force-push/delete; `staging` exige os mesmos 3 checks, sem PR obrigatória. Push direto para `main` testado de propósito e rejeitado pelo GitHub (`GH006`), confirmando a trava. Nova skill `.agents/skills/kortex-environment-guardian/SKILL.md` referenciada em `AGENTS.md` (seção "Processo MAS") como gate de toda promoção entre branches, encadeando `kortex-delivery-guardian` como checklist final pré-merge em `main` | Fecha definitivamente o item (c) de DEC-28. Não reabre DEC-20 a DEC-29. Atualiza `docs/legacy/mvp-tecnico/PROJECT_STATE.md` (remove Gap #4, arquivo já reorganizado por DEC-29) e `docs/KORTEXOS_5_1_2_TRUTH_MAP.md` (ponto cego "ambiente de homologação inexistente" deixa de ser verdade) |

Pendências do A1 resolvidas por benchmark (justificativas na seção 8):

| # | Pendência | Resolução canônica |
|---:|---|---|
| DEC-06 | Ordem de consumo no split de pagamento | benefício → wallet → forma externa. Prática de mercado: créditos de membership aplicam automaticamente antes do pagamento |
| DEC-07 | Breakage de pacote expirado | **SUPERADA por DEC-14** (seção 0) — mantida aqui apenas como histórico da primeira formulação |
| DEC-08 | Overuse de plano | Configurável por plano: excedente a preço de membro (retenção) ou avulso (proteção de margem). Default do sistema: preço avulso; o plano declara explicitamente se oferece preço de membro |
| DEC-09 | Rateio de desconto (combo/pacote) | Confirmado: proporcional ao preço base de cada item, regra única. Comissão e reconhecimento calculam sobre o valor rateado |
| DEC-10 | Preço de referência de consumo de plano | Confirmado obrigatório por item do plano. Sem ele, Gate 14 é incalculável |

---


---

# SEÇÃO 2 — REGISTRO D-01 A D-09 (anexo A1, decisões conceituais originais)

**Status geral:** das 9 decisões originais, todas foram resolvidas ou substituídas por DEC-01 a DEC-19. D-07, a última em aberto, foi resolvida por DEC-19 em 2026-07-19.

| # | Decisão | Recomendação | Risco de adiar |
|---:|---|---|---|
| D-01a | Duração própria por profissional no MVP | SIM | Agenda mente sobre capacidade real |
| D-01b | Preço por nível de profissional no MVP | NÃO (adiar) | Sofisticação prematura; complica comissão |
| D-02 | Taxa da gorjeta em cartão | Salão absorve; custo visível no ledger | Tip isolation vira mentira contábil |
| D-03 | Ordem de consumo no split | benefício → wallet → forma externa | Checkout ambíguo; disputa com cliente |
| D-04 | Rateio de desconto (combo/pacote) | Proporcional ao preço base, regra única | Comissão ambígua → conflito com staff |
| D-05 | Preço de referência de consumo de plano | Obrigatório por item do plano | Gate 14 incalculável; comissão vira briga |
| D-06 | Breakage de pacote expirado | Reconhece receita na expiração, por regra | Passivo eterno ou receita fantasma |
| D-07 | Anamnese/dados sensíveis | Adiar para D26 com base legal | Risco LGPD se entrar no cadastro base |
| D-08 | Overuse de plano | Excedente paga avulso (default) | Plano canibaliza receita cheia |
| D-09 | Comissão de add-on | Própria e explícita, não herdada | Comissão implícita gera erro silencioso |

---


### Reconciliação D → DEC

| D-XX | Status atual | Resolvido/substituído por |
|---|---|---|
| D-01a | CONFIRMADO | Duração própria por profissional: SIM (consistente com DEC-04) |
| D-01b | SUPERADA | DEC-04 reverteu a recomendação — preço por nível é produto final, não adiado |
| D-02 | SUPERADA | DEC-01 (absorção de taxa da gorjeta segue o modelo da forma de pagamento) |
| D-03 | SUPERADA | DEC-06 (ordem de consumo: benefício → wallet → forma externa) |
| D-04 | CONFIRMADA | DEC-09 confirma rateio proporcional como regra única |
| D-05 | CONFIRMADA | Preço de referência de consumo por item — mantido obrigatório; base reaproveitada por DEC-18 para reembolso |
| D-06 | SUPERADA (duas vezes) | DEC-14 primeiro, depois DEC-18 unificou com a regra de planos |
| **D-07** | **RESOLVIDA** | DEC-19 (2026-07-19) — base legal art. 11,I; módulo isolado em Parte II §13/D26 |
| D-08 | SUPERADA | DEC-18 unificou overuse/desistência na fórmula única de reembolso |
| D-09 | CONFIRMADA | Comissão de add-on própria e explícita, sem alterações |

---

# SEÇÃO 3 — DELTAS DE CADASTRO (anexo A1 → 5.1.1, histórico)

O corpo de cadastros do anexo A1 (formas/tipos de pagamento, clientes, staff, serviços, produtos, complementos, combos, planos, pacotes) permanece válido com os seguintes deltas vinculantes:

| # | Delta | Efeito |
|---:|---|---|
| 1 | DEC-04 substitui D-01b | Preço/tempo/comissão por nível e por profissional entram no produto final. A matriz staff × serviço ganha os três eixos de override |
| 2 | DEC-01 substitui D-02 | Campo "aceita gorjeta" da forma de pagamento passa a referenciar o modelo de absorção da própria forma |
| 3 | DEC-03 adiciona estados | Cadastro/ciclo da comanda ganha `reaberta` e `refechada`; travas da seção 4.3 |
| 4 | DEC-06 fecha D-03 | Ordem de consumo no split: benefício → wallet → forma externa |
| 5 | DEC-14 fecha D-06 (substitui DEC-07) | Pacote exige validade + sequência de 4 estágios (aceite digital → uso → extensão → breakage) + política de reembolso por desistência antecipada configurável; validação jurídica antes de produção comercial |
| 6 | DEC-08 fecha D-08 | Overuse configurável por plano; default avulso |
| 7 | Escopo produto final | Nenhum campo do A1 é cortado por "ser MVP"; a ordem de construção continua regida pelo Master 5.1.1 (seção 22) |

---


---

# SEÇÃO 4 — EVIDÊNCIAS DE BENCHMARK (pesquisa que fundamentou DEC-01, DEC-02, DEC-04, DEC-06, DEC-11 a DEC-15)

### 8.1 Players internacionais

| Tema | Prática observada | Uso no KortexOS™ |
|---|---|---|
| Reabertura de venda fechada | Zenoti permite desbloquear/editar fatura pós-fechamento apenas com permissões específicas; fatura sob trava financeira não pode ser desbloqueada; fechamento diário lista voids | Valida o modelo permissão + janela + financial lock (seção 4.3) |
| Split de gorjeta | Vagaro e Mangomint fazem split automático proporcional ao preço do serviço de cada profissional como default, com split manual disponível; Square oferece pooling por horas/função | Valida DEC-02: valor como default, tempo como política, manual como exceção |
| Preço/tempo por profissional | Phorest ajusta automaticamente tempo e preço por indivíduo; segmentação por nível é prática comum no mercado premium | Valida DEC-04 |
| Herança org → unidade | Zenoti define configurações na organização e lista explicitamente quais podem ser sobrepostas por unidade, com flag "override para esta unidade" e cadeias de fallback | Valida a semântica ponteiro + override autorizado por campo (seção 1.6) |
| Consumo de benefício no checkout | Créditos de membership aplicam automaticamente no checkout antes de pagamento externo | Valida DEC-06 |
| CDC / validade de pacote | Cláusulas que colocam o consumidor em desvantagem exagerada são nulas (art. 51); direito a reembolso protegido | Fundamenta DEC-07 e o flag de validação jurídica |

### 8.2 Players nacionais — Trinks, AppBarber, CashBarber

| Player | Prática observada | Leitura KortexOS™ |
|---|---|---|
| AppBarber | **CORRIGIDO por evidência primária (telas em produção, 2026-07):** reabertura e edição de comanda são FLUXO PADRÃO — comanda finalizada oferece "Reabrir agendamento" imediatamente, com edição plena (itens, valores, comissão, pagamentos múltiplos). O bloqueio existe apenas DEPOIS: caixa finalizado (valor faturado no financeiro) ou comanda dentro de pagamento de comissão | Reabrir+editar é comportamento base do mercado nacional, não exceção — DEC-11 o torna obrigatório no KortexOS™. As travas posteriores (caixa, comissão) CONFIRMAM a seção 4.3. Diferencial KortexOS™: a mesma liberdade operacional, mas por versões no ledger (v1→v2) em vez de mutação da história |
| AppBarber | Recibo de comissão com três opções de taxa de cartão: não descontar, descontar ou dividir com o profissional; sem taxas de bandeira cadastradas, valores líquido/descontado não calculam | CONFIRMA DEC-01 (os três modelos de absorção já operam no mercado nacional) e o invariante "forma sem taxa cadastrada = margem cega" |
| AppBarber | Clube de Assinaturas por quota (ex.: 2 cortes + 2 barbas/mês); excedente é cobrado normalmente; fatura vencida sem pagamento → serviços voltam a preço cheio | CONFIRMA DEC-08 (default avulso) e a suspensão de benefício por inadimplência (dunning) |
| AppBarber | Comissão de assinatura: percentual reduzido sobre o preço de tabela do serviço, ou faixas por produtividade | O preço de referência da DEC-10 SUBSUME esse modelo (referência = preço de tabela × fator). KortexOS™ mantém referência por item como base canônica única |
| AppBarber | Modal "Editar Item" com valor unitário, comissão %, cortesia e exclusão editáveis; parâmetro do sistema controla se o campo de comissão aparece, bloqueia ou oculta por perfil | Realidade obrigatória absorvida pela DEC-12. Diferencial KortexOS™: a mesma edição existe, porém GOVERNADA — permissão por persona (padrão que o próprio AppBarber já esboça no parâmetro), motivo registrado e trilha de auditoria; nunca edição silenciosa. Ajuste FORA da alçada da persona vira Action Request (Master 19.2) |
| AppBarber | Pacote associado à comanda desconta sessão; parâmetro para não gerar comissão em serviço de pacote; pacote e clube são mutuamente exclusivos por cliente | A ambiguidade de comissão em pacote que a DEC-10 elimina; a exclusividade pacote×plano é limitação que o KortexOS™ NÃO herda (obrigações coexistem com origem própria) |
| Trinks | Edição/exclusão de serviço direto em fechamento e alteração retroativa de data de movimentação | ANTI-PATTERN central: história financeira mutável. KortexOS™ entrega a mesma flexibilidade operacional via reabertura por versões, sem destruir auditabilidade |
| Trinks | Cancelar comanda libera o número, mas o serviço permanece registrado | Compatível com o invariante "nada é apagado" (seção 4.1) |
| Trinks | Crédito de cliente nativo: registrar, usar no fechamento, troco vira crédito, usar crédito de outro cliente, VENDER crédito por link de pagamento; débito (dívida) nativo | Valida client wallet + fiado governado. Duas práticas a ABSORVER: venda de crédito de wallet por link (KortexLink + wallet top-up) e uso de crédito de outro cliente (resolvido no KortexOS™ via household/pagador≠beneficiário, Gate 07) |
| Trinks | Profissional Parceiro (Lei do Salão Parceiro), assistente por serviço, módulo de anamnese, alerta ao fechar conta com serviço não finalizado | Valida vínculo Parceiro no cadastro de staff, percentual de assistente declarado (seção 5.2) e anamnese isolada em D26 |
| Trinks | Rodízio de profissionais (fila de atendimento por ordem) | LACUNA IDENTIFICADA no KortexOS™ — ver 8.3 |
| CashBarber | Posicionamento assinatura-first; planos com personalização de quantidade, procedimento e dias da semana | CONFIRMA a janela de uso por dias fracos (Master 14.2, plano terça–quinta) como demanda real do mercado nacional |
| CashBarber | NÃO distribui comissão de assinatura aos barbeiros nativamente (depende de app externo do ecossistema do fornecedor) | LACUNA DO LÍDER NACIONAL DE ASSINATURA: comissão de consumo de plano é exatamente o que DEC-10 + Gate 14 resolvem nativamente. Diferencial competitivo direto do KortexOS™ |
| CashBarber | Barbeiro lança serviços/produtos na própria comanda pelo celular | Compatível com persona Profissional (Master 18.1), sempre via Command |

### 8.3 Lacuna nova identificada no benchmark nacional

| Item | Decisão |
|---|---|
| **Rodízio / fila de walk-in** | Distribuição justa de clientes por ordem de chegada entre profissionais disponíveis (realidade diária de barbearia, presente na Trinks). O Master 5.1.1 cobre waitlist de agendamento, mas NÃO cobre fila de walk-in com rodízio. Registrar como evolução de D07/D08 na promoção 5.1.2. Não especificar solução neste anexo — apenas a lacuna. |

### 8.4 Realidades operacionais do AppBarber a preservar (evidência primária — telas em produção)

| Realidade | Destino no KortexOS™ |
|---|---|
| Split de pagamento nativo na comanda (Pagamento + Segundo Pagamento/Bandeira) | Já canônico (split multi-forma, DEC-06) |
| Chips de ação na comanda: +produto, +serviço, +pagamento, +desconto, +gorjeta | Padrão de superfície do checkout (UI/UX Spec; sem cálculo no frontend) |
| Prontuário do atendimento com material, tipo de corte, observação e fotos | Ficha técnica do cliente: D26 (dado potencialmente sensível, base legal e consentimento antes de fotos) |
| Status visível na agenda: assinatura ativa/inadimplente, multi, pacote, cupom, aniversário, pago online | Projeções de benefício/pagamento na agenda (D08/D18/D19) |
| Flag "não quer conversar" | Supressão de contato no Contact & Policy Orchestrator (Master 12.8) — consentimento e fadiga |
| Encaixe (overbooking manual) | Evolução de D07/D08: encaixe governado por permissão, registrado como exceção de capacidade |
| Pós-fechamento: mensagem de agradecimento, prontuário, enviar recibo | KortexLink transacional consentido |

```text
Dados de benchmark são referência, não verdade de produto (RAGOV 20.4).
O Global Benchmark Map formal (etapa 3 da ordem de construção) continua PENDENTE
e não é substituído por esta síntese.
```

---


---

# SEÇÃO 5 — VEREDITO RED TEAM (Parte I — aprovação do 5.1.1 como fonte canônica)

### 25.1 Aprovado como tese canônica

| Item | Veredito |
|---|---|
| KortexOS™ como nome canônico | APROVADO |
| Intelligent Capacity thesis | APROVADO |
| Ecossistema das 5 soluções | APROVADO |
| KortexFlow | APROVADO como tese; implementação pendente |
| Kortex.ai sem soberania | APROVADO |
| KortexLink como circuito de ativação | APROVADO |
| Negative Guard expandido | APROVADO |
| No-show progressivo + Trust Pass + Healing | APROVADO |
| Yield & Occupancy | APROVADO |
| Subscription Engine | APROVADO como motor central |
| Corporate Benefits | APROVADO como motor obrigatório |
| Partner Benefits | APROVADO como motor obrigatório |
| Kortex Autonomous Operations Engine | APROVADO como engine transversal |
| Modelo math-first no PostgreSQL | APROVADO como prioridade |
| Waitlist Recovery com hold | APROVADO P1 |
| Contact & Policy Orchestrator | APROVADO como obrigatório |
| Incrementality Engine | APROVADO por evolução |

### 25.2 Bloqueado

| Item | Motivo |
|---|---|
| Blueprint imediato | Falta benchmark, comparative proposal, truth map e migration map |
| SQL novo | Falta Blueprint aprovado |
| IA executora | Viola soberania backend |
| Plano sem ledger/wallet | Cria saldo paralelo |
| Corporativo sem privacidade | Risco LGPD |
| Parceria como cupom aberto | Perde rastreabilidade e margem |
| Dynamic pricing automático | Precisa política, dados e gate |
| Bot/LLM como cérebro de decisão | Viola soberania backend |
| Automação sem modo OBSERVE | Risco não mensurado |
| RL no MVP | Complexidade e risco sem evidência |

### 25.3 Risco principal

```text
O maior risco do KortexOS™ 5.1.1 não é falta de visão.
É automatizar correlações ruins antes de provar dados, causalidade, margem e segurança.
```

### 25.4 Decisão final

```text
Master Briefing KortexOS™ 5.1.1 aprovado como fonte canônica após aprovação do Platform Owner.
A versão 5.1 anterior torna-se histórica e é substituída por esta reescrita.
Blueprint KortexOS™ 5.1.1 continua bloqueado até benchmark formal, proposta comparativa, Truth Map e Migration Map.
SQL e automação em produção continuam bloqueados.
```

---


---

# SEÇÃO 6 — VEREDITO RED TEAM (anexo A2 — configuração, onboarding, comanda, gorjeta, níveis)

| Item | Veredito |
|---|---|
| Renomear D02 → Business Configuration & Policy Layer | APROVADO (RENOMEAR) |
| Herança ponteiro + override autorizado por campo | APROVADO |
| Onboarding sem persistência própria | APROVADO |
| Reabertura por versões + financial lock | APROVADO |
| Gorjeta com absorção da forma + split valor/tempo | APROVADO |
| Níveis com cascata tripla de resolução | APROVADO |
| Risco nº 1 | Reabertura de comanda é o ponto de maior risco contábil do sistema: sem a cascata completa de reversão (comissão, benefício, estoque, gorjeta, pagamento), cria dupla verdade financeira. Gate antes de produção é inegociável |
| Risco nº 2 | Breakage de pacote sem validação jurídica pode gerar passivo CDC. Não ativar comercialmente antes do parecer |
| Risco nº 3 | Este anexo não pode virar documento paralelo permanente. Incorporar no 5.1.2 por reescrita integral das seções afetadas |
| Oportunidade nº 1 | O líder nacional de assinatura não comissiona consumo de plano nativamente. DEC-10 + Gate 14 são diferencial competitivo direto — proteger na comunicação de produto |
| Lacuna registrada | Rodízio/fila de walk-in (8.3) entra na pauta da promoção 5.1.2 como evolução de D07/D08 |

---


---

# SEÇÃO 7 — ENCERRAMENTO DO ANEXO A1 (histórico, 2026-07-19)

Este anexo não altera domínios, gates nem a ordem de construção do Master Briefing 5.1. Ele aprofunda a especificação conceitual dos cadastros para que Benchmark (etapa 3), Truth Map (etapa 5) e Blueprint (etapa 7) tenham base concreta de comparação e materialização.

```text
Cadastro é a matéria-prima do cálculo.
Cálculo sem cadastro completo é chute com interface.
Nenhuma venda de plano ou pacote antes do ledger.
```


---
---


---

# SEÇÃO 8 — PRÓXIMO PASSO DO ANEXO A2 (histórico — CONCLUÍDO em 2026-07-19)

**Status: CONCLUÍDO.** O passo abaixo foi executado — anexo A2 aprovado e incorporado ao Master Briefing 5.1.2 na mesma sessão. Preservado apenas como registro histórico.

```text
1. Platform Owner aprova ou veta DEC-01 a DEC-10 (já refletem suas decisões verbais).
2. Incorporar este anexo no Master Briefing 5.1.2 por reescrita integral
   (D02, D03, D05, D06, D12, D14, D15, D17 e cadastros).
3. Só então: Global Benchmark Map formal (etapa 3), que segue PENDENTE.
Nada deste anexo autoriza SQL, migration ou tela.
```

---

*Fim das SEÇÕES 1-8 (registro original). Para a regra vigente de qualquer tema aqui referenciado, consulte `KORTEXOS_5_1_2_MASTER_BRIEFING_CANONICO.md`. O registro append-only continua abaixo (SEÇÕES 9-14) — este marcador é histórico, não o fim real do arquivo; nota adicionada em 2026-07-28 para evitar leitura truncada.*

---

# SEÇÃO 9 — ADENDO APPEND-ONLY (2026-07-26)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-38 | Regularização da Etapa 8 da Onda 1 e autorização do plano corretivo forward-only | **Aprovado pelo Platform Owner em 2026-07-26.** Regulariza formalmente a autorização da Etapa 8 da Onda 1 que faltou no registro de DEC-35/DEC-36: a autorização vale exclusivamente para projetar, criar e validar migrations, backend, frontend e testes **forward-only** em ambiente local/descartável e em `staging`, dentro do PRD corretivo e das fatias `issues/006` a `issues/011`. Não torna retroativa a ausência do registro, não edita migrations já aplicadas e não autoriza `main` nem produção. Correção factual: o reset limpo do commit `0f4a5d9` aplica **21 migrations**, não 23; DEC-37 registra a evidência então relatada, mas não representa fechamento do contrato completo de vínculo depósito↔agendamento↔comanda. A arquitetura aprovada exige: (1) checkout de agendamento por endpoint server-owned `POST /appointments/:id/checkout`, derivando a identidade do agendamento/hold e mantendo o checkout sem agendamento para walk-in; (2) `in_service` ou `completed` como únicos estados elegíveis; (3) identidade do hold imutável — organização, unidade, agendamento, cliente, serviço e profissional — e bloqueio de mutação de cliente/serviço/profissional enquanto houver hold ativo; (4) vínculo relacional persistido entre comanda, agendamento e hold, com locks e CAS fail-closed; (5) cancelamento/reagendamento/no-show atômicos, sendo cancelamento de `immediate_charge` bloqueado enquanto não houver estorno real; (6) evento PSP com identidade externa não ambígua e dead-letter reprocessável; e (7) expiração reativa/fail-closed sem scheduler novo. O adendo ao Blueprint e ADR 0018 registram o desenho; cada fatia requer `$tdd`, Red Team e evidência própria antes de integração. | Qualifica prospectivamente DEC-35/DEC-36/DEC-37 quanto ao escopo necessário para completar a Onda 1. Não invalida o histórico nem autoriza promoção. |

**Continuação append-only do índice de status:** DEC-20 a DEC-38 cobrem agora as decisões de processo/auditoria; DEC-38 é a única autorização vigente para a implementação corretiva desta Onda 1 e não altera o `NO-GO` de promoção.

---

# SEÇÃO 10 — EVIDÊNCIA APPEND-ONLY (2026-07-26)

| # | Registro | Conteúdo |
|---:|---|---|
| DEC-39 | Execução corretiva local da Onda 1 | As fatias autorizadas por DEC-38 foram implementadas e validadas em banco Supabase local descartável: identidade financeira imutável do hold; checkout de agendamento server-owned com vínculo persistido; transições `released`/`expired`; replanejamento idempotente; webhook dead-letter reprocessável; e testes de regressão. Evidência registrada em `KORTEXOS_5_1_2_ONDA_1_REMEDIATION_VERIFICATION_2026_07_26.md`. Este registro fecha os três blockers técnicos de DEC-36 no ambiente local, mas **não** muda o `NO-GO`: a migration falha fechada se houver holds legados sem identidade inferível e exige preflight/homologação em `staging`, seguida de Environment Guardian e Delivery Guardian, antes de qualquer promoção. |
| DEC-40 | Homologação da Onda 1 bloqueada por infraestrutura de staging | Em 2026-07-26, a CI do commit `0803ac2` concluiu com sucesso nos três jobs obrigatórios. A separação de ambiente foi conferida: `staging` aponta para serviços Render e projeto Supabase próprios. Entretanto, `kortex-api-staging` está suspenso por billing no Render e o último deploy live do backend é `9abada1`, anterior à correção. A PWA de staging respondeu, mas não se pode homologar checkout, lifecycle, migrations ou webhooks sem backend ativo. Nenhuma migration foi aplicada nem dado de staging foi alterado. A retomada exige reativar o backend, comprovar o deploy do commit, executar preflight dos holds legados e os cenários funcionais; o `NO-GO` para `main`/produção permanece. |

---

**Nota de restauração (2026-07-28, ver DEC-47):** esta SEÇÃO 9 (linha DEC-38) e o título/cabeçalho da SEÇÃO 10 foram removidos do arquivo pelo commit `7df2eba` (2026-07-27), violando a regra append-only declarada na linha 5 deste documento — as linhas de DEC-39/DEC-40 sobreviveram como fragmento de tabela sem cabeçalho, soltas logo após o marcador "Fim do Decision Log". Restaurado por leitura direta de `git show 7df2eba^:docs/architecture/governance/KORTEXOS_5_1_2_DECISION_LOG.md`, texto idêntico ao original. Nenhuma decisão é alterada por esta restauração — apenas o rastro dela deixa de estar apagado.

---

# SEÇÃO 11 — DEC-43: PROTOCOLO DE AUTOMAÇÃO DOCUMENTAL (2026-07-27)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-43 | Docs-as-Code e automação documental para agentes | **Aprovado pelo Platform Owner em 2026-07-27.** A cadeia primária de agentes passa a exigir classificação Diátaxis para todo Markdown novo, frontmatter YAML para novos artefatos em `docs/` não legado e `issues/`, atualização em cadeia de issue/Índice/matriz DEC↔ADR, declaração explícita de supersessão e checklist documental em todo handoff aplicável. Os documentos existentes só são movidos por migração explícita com atualização de links; esta decisão não autoriza apagar conteúdo, alterar regras de negócio ou promover ambientes. Especificação operacional: [Protocolo de Automação Documental](KORTEXOS_DOCUMENTATION_AUTOMATION_PROTOCOL.md). | Institui governança documental prospectiva; não revoga DEC/ADR de produto nem substitui o Truth Map. |

---

# SEÇÃO 12 — DEC-46: APROVAÇÃO DO BLUEPRINT DA ONDA 3 — COMPENSATION (2026-07-27)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-46 | Aprovação do Blueprint da Onda 3 (Compensation: Staff Levels & Comissão de Venda) | **Aprovado pelo Platform Owner em 2026-07-27.** Blueprint materializa exatamente a tabela da Onda 3 no Migration Map v1.2 §3 (D17/D05): `staff_levels`/`staff_level_service_overrides` (DEC-04, override de preço/tempo/comissão por nível de carreira) e `private.resolve_sale_commission()`/`commission_sale_records` (DEC-15, comissão de venda de pacote, independente da comissão de execução). `private.resolve_commission()`, `checkout_close` e `create_appointment` permanecem intocados — fundação sem ativação, mesmo padrão da Onda 2 (ADR 0019). **Escopo confirmado com o Platform Owner antes da redação:** estritamente o Migration Map aprovado (DEC-24/27/28) — Cash Sessions/Void (ADR 0007) e Comissão Escalonada (ADR 0003, ainda `Proposed`) ficam de fora, apesar de a DEC-29 os mencionar como "re-hospedados" em Onda 3 por tema; nenhum dos dois é objeto da tabela aprovada, e ficam para onda própria futura com adendo ao Migration Map, mesmo padrão do gap do D02 (DEC-25). **Decisões de desenho fechadas:** `staff_level_id` nullable com desvio registrado da regra "Obrigatório" do Master §6.1 (obrigatoriedade vira validação de aplicação na onda de ativação, não `CHECK` de banco, para não travar organizações existentes contra um cadastro de nível ainda vazio); nova função `resolve_service_pricing()` para a cascata de preço/tempo, sem call site nesta Onda; comissão por nível gravada em `staff_level_service_overrides` mas não consumida por `resolve_commission()` (marcada intocada pelo Migration Map §4); `commission_sale_record_create()` construída e testável isoladamente, mas sem call site automático — o payload de `checkout_close` não carrega "quem vendeu o pacote" (distinto de quem executa), e nenhum default incorreto foi assumido; sem postagem em `kortex_ledger_entries` nesta Onda (ledger da Onda 2 segue sem produtor real, `NO-GO`); clawback (DEC-18) não automatizado, mesmo padrão de `order_void` (ADR 0007). Primeira Onda sob o protocolo DEC-44: Feature Flag em `organizations.settings`, Pre-flight Check por migration, TDD com pgTAP/Jest, rastreabilidade via `closes issues/NNN`. **Red team de desenho, 2 rodadas independentes:** 1ª `NO-GO` — 1 achado crítico (`staff_level_id` nullable contradizendo "Obrigatório" sem desvio registrado, corrigido conforme acima) e 6 achados menores (alegação incorreta de que `create_appointment` não consome `professional_service_capabilities.duration_override_minutes` — na verdade já consome, só o preço não é usado por `checkout_close`; citação de linha de `checkout_close` apontando para migration já superada; unicidade proposta em `commission_sale_records` bloquearia venda repetida legítima do mesmo pacote no mesmo pedido, removida em favor de `idempotency_key`; FKs sem `on delete` e `order_id` sem o padrão de 3 colunas já usado por `order_items`/`payments`; `search_path` ausente em 2 funções `security definer`; índice secundário faltando em `staff_level_service_overrides`; precedente `no_show_settlement_create` citado incorretamente — RPC abandonada, não "aguardando ativação" —, substituído por `deposit_hold_create`, que segue ativa em produção). Todos os 7 corrigidos; 2ª rodada `GO`. Ver [ADR 0020](../adr/0020-onda3-compensation-staff-levels-sale-commission.md) para o detalhamento completo e as alternativas rejeitadas. **Nota de correção documental:** uma cópia intermediária do Blueprint/ADR 0020 (anterior à 2ª rodada de red team, ainda com veredito pendente) havia sido mesclada em `staging` via PR #22 por engano de coordenação entre sessões concorrentes no mesmo diretório de trabalho; este registro substitui essa cópia pela versão final corrigida e efetivamente aprovada — nenhuma decisão de produto muda, só a redação chega ao estado correto. O vínculo DEC-44↔ADR-0020 registrado anteriormente na matriz cruzada estava equivocado (DEC-44 é o protocolo de otimização de código, não esta Onda) e foi corrigido na mesma revisão. | Libera o Fatiamento (issues `017`-`020`) como próximo passo executável. Etapa 8 (SQL) continua exigindo autorização própria do Platform Owner, não incluída nesta aprovação. Não reabre DEC-24/27/28/33. |

---

# SEÇÃO 13 — DEC-47: REGULARIZAÇÃO DA ETAPA 8 E DA PROMOÇÃO A `STAGING` DA ONDA 3 (2026-07-28)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-47 | Regularização da Etapa 8 (SQL) e da promoção a `staging` das fatias 017-020 da Onda 3 | **Registrado pelo Platform Owner em 2026-07-28**, mesmo padrão de DEC-38/DEC-42: a Etapa 8 desta Onda foi iniciada e concluída por instruções diretas do Platform Owner ("implementar as fatias 018-020", precedida de "implementar a fatia 017 com $tdd" numa sessão anterior) **sem o registro formal próprio que DEC-33/DEC-46 exigem** — DEC-46 (SEÇÃO 12) exclui explicitamente a Etapa 8 de sua aprovação. **Diferença material em relação a DEC-38/DEC-42: desta vez a lacuna não parou no ambiente local.** O Platform Owner pediu explicitamente "push" (fatia 017, sessão anterior) e depois "merge o PR" (fatias 018-020, PR #24) — o merge em `staging` (commit `67083b1`) também não teve autorização formal prévia, ultrapassando o limite que DEC-42 havia imposto à Onda 2 ("Não autoriza `staging` nem `main`/produção"). **Regulariza retroativamente o registro, não a ausência de rigor técnico:** as 4 fatias seguiram `$tdd` (pgTAP RED antes de cada migration, GREEN depois), suíte completa sem regressão a cada passo (647/647 na fatia 020) e CI da PR #24 verde nos 3 jobs obrigatórios — evidência bruta, comandos reais, conferidos nesta sessão contra o repositório, não aceitos por relato. **Achados de uma auditoria independente do Platform Owner, conferidos pessoalmente nesta sessão contra o código antes deste registro (disciplina DEC-33):** confirmados P1 (`commission_sale_record_create` não valida que o pacote vendido pertence ao pedido informado — o vínculo é estruturalmente inexistente, `order_items` não carrega `package_id`), P2 (`commission_sale_records` sem proteção de mutação — `service_role` herda DML completo via `alter default privileges`, nenhum guard de imutabilidade como o de `deposit_holds`), P3 (pre-flight da fatia 020 não confere `organizations`/`professionals`) e a contradição documental entre `docs/INDEX.md`/`issues/README.md` (diziam "local, não promovida") e o git (já mesclado em `staging`) — o commit que escreveu essa frase viajou dentro do próprio PR que promoveu. Verificado adicionalmente, fora do escopo da auditoria recebida: o projeto Supabase de produção (`kpedsuklnedlhjvadiyc`) segue em `20260720223256` (Fase 11) — nenhuma migration das Ondas 0-3 chegou a produção, o que reduz o risco de P1/P2 a latente, não exposto; e o Decision Log havia perdido a SEÇÃO 9 (linha de DEC-38) e o cabeçalho da SEÇÃO 10 por edição não append-only do commit `7df2eba`, restaurados nesta sessão (ver nota após SEÇÃO 10). **Escopo desta autorização:** regulariza prospectivamente a Etapa 8 local das fatias 017-020 e sua presença em `staging`. **Não autoriza `main`/produção** — isso continua exigindo os gates de ambiente (Environment Guardian, Delivery Guardian) e homologação, hoje bloqueados por infraestrutura (DEC-40, `kortex-api-staging` suspenso por billing). **Condiciona a continuidade da Onda:** P1 e P2 devem ser corrigidos por fatia própria (021 e 022, ver DEC-48) antes de qualquer nova capability ser ativada sobre esta fundação; P3 é corrigido na mesma fatia 022. | Regulariza prospectivamente a Etapa 8 e a promoção a `staging` da Onda 3, mesmo efeito de DEC-38/DEC-42 estendido ao ambiente. Não invalida o histórico das fatias 017-020 nem autoriza promoção a `main`/produção — essa continua exigindo os gates de ambiente e homologação próprios. Não reabre DEC-24/27/28/33/46. |

---

# SEÇÃO 14 — DEC-48: EMENDA DA FRONTEIRA `CHECKOUT_CLOSE` PARA A FATIA 021 (2026-07-28)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-48 | Emenda ao Blueprint/ADR 0020 — autoriza tocar `checkout_close` para persistir o vínculo pacote↔pedido (fatia 021) | **Aprovado pelo Platform Owner em 2026-07-28**, em resposta ao achado P1 de DEC-47: hoje é estruturalmente impossível saber, pelo banco, que pacote foi vendido em qual pedido — `order_items.kind` só aceita `service`/`product` (`mvp_baseline.sql`), e `checkout_close` dissolve cada pacote em N linhas de serviço, descartando o `package_id` (`20260726190000_onda1_checkout_close_deposit_appointment_guard.sql:276-284`). Isso torna `commission_sale_record_create` capaz de aceitar qualquer pacote da organização contra qualquer pedido fechado, e calcula a comissão sobre `packages.price_cents` (preço de tabela) em vez do valor efetivamente cobrado no pedido. **Duas alternativas de correção foram avaliadas e a de maior retrabalho futuro foi descartada:** (A) uma guarda por composição, validando `package_items`×`order_items` sem alterar `checkout_close` — rejeitada por ser heurística com falsos positivos conhecidos (pacote com os mesmos serviços vendidos avulsos; pacotes sobrepostos; incapaz de distinguir 1 de 2 vendas do mesmo pacote) e por não corrigir a base de cálculo da comissão; seria descartada por completo na Onda de ativação. (B) persistir o vínculo real — **escolhida**, por ser infraestrutura permanente que a Onda de ativação exigiria de qualquer forma (inclusive para relatório de venda de pacote, hoje também impossível). **Escopo estritamente autorizado, emendando o Blueprint Onda 3 §1/§3.2 ("esta Onda... não modifica `checkout_close` nem `create_appointment`") e a ADR 0020 (mesma cláusula):** (1) coluna `order_items.package_id` nullable + FK composta `(organization_id, package_id) references packages(organization_id, id) on delete restrict`, aditiva, sem novo valor de `kind` (evita reabrir o `CHECK` e o risco de dupla contagem de total); (2) `checkout_close` redefinida (nova migration, nunca editando a já aplicada — padrão já usado 5 vezes no projeto) só para gravar `package_id = v_ref_id` no INSERT do ramo de pacote já existente — nenhuma mudança em preço, rateio por maior-resto, totais ou reconciliação de depósito; (3) `commission_sale_record_create` passa a validar que o pedido contém itens daquele pacote e a calcular a comissão sobre `sum(order_items.total_cents) where package_id = X` (valor cobrado, não preço de tabela). **Por que a emenda é aceitável apesar do precedente de risco de DEC-36 (fatia 004, único bug crítico pós-merge do projeto até hoje, também por tocar `checkout_close`):** o raio de explosão é qualitativamente menor — grava um valor num INSERT que já existe, não recalcula preço, rateio, total ou reconciliação; e a Onda 3 nunca chegou perto de produção (produção segue em Fase 11, DEC-47), então não há usuário real exposto durante a correção. **Não retroage:** pedidos históricos não têm como ser reconstruídos — a informação foi descartada no checkout, não apenas não-gravada; sem backfill possível. | Emenda pontual do Blueprint Onda 3 §1/§3.2 e da ADR 0020 (seção "Decision" e "Consequences"), especificamente e apenas para o escopo descrito — não reabre nenhuma outra decisão fechada da Onda 3 (DEC-04/15/18/24/28), não autoriza tocar `create_appointment`, e não altera `resolve_commission()` (Migration Map §4, decisão 2, continua intocada). Autoriza a Etapa 8 da fatia 021 sob o mesmo regime de DEC-47 — local/`staging`, não `main`/produção. |

---

# SEÇÃO 15 — DEC-49: APROVAÇÃO DO BLUEPRINT DA ONDA 4 — CALENDAR POLICY, AVAILABILITY RESOLVER & RESOURCE ORCHESTRATION (2026-07-29)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-49 | Aprovação do Blueprint da Onda 4 (Calendar Policy, Availability Resolver, Resource Orchestration) | **Aprovado pelo Platform Owner em 2026-07-29.** Blueprint materializa exatamente a tabela da Onda 4 no Migration Map v1.2 §3 (D02/D07/D11): `calendar_policies`/`professional_shifts` (horário padrão e turno, versionados com vigência), `calendar_holidays`/`calendar_exceptions`/`calendar_time_off` (feriados, exceções pontuais/fechamento/abertura excepcional, folgas — cobrindo os 8 objetos de política do Master §2.2), `calendar_policy_conflicts` (fila de conflitos, Master §2.4 CRÍTICO), `resources`/`resource_locks` (D11, concorrência otimista equivalente a `appointments`) e o Availability Resolver (D07, funções SQL pequenas + orquestração em Express, sem tabela própria — Migration Map §4 decisão 3). `create_appointment` e `checkout_close` permanecem intocados — fundação sem ativação, terceira vez que este padrão se repete na trilha (Onda 2, ADR 0019; Onda 3, ADR 0020). **Decisões de desenho fechadas por interview (`$grill-me`) antes da redação, todas registradas na ADR 0022:** escopo desta Onda é fundação sem ativação (Resolver exposto por rota nova isolada, não por modificação de RPC crítica); arquitetura do Resolver é Rota A (funções SQL pequenas + loop de slots em Express, não uma RPC monolítica — o motivo que levou `appointments` à Rota B na ADR 0012, proteger invariante transacional multi-tabela numa escrita, não se aplica a uma leitura pura); `calendar_policies`/`professional_shifts` versionados por linhas com vigência (`valid_from`/`valid_to`), não ponteiro simples como `staff_levels` — a exigência do Master aqui é mais forte (histórico da própria configuração, não só do appointment que a consumiu); fila de conflitos populada por trigger automático no `INSERT` de política, nunca por varredura manual; `resource_locks.appointment_id` nullable, aceitando bloqueio manual/manutenção além do vínculo a um agendamento; `calendar_holidays` nasce vazia, sem seed de calendário nacional. **Red team de desenho, 2 rodadas:** 1ª autoavaliação corrigiu 2 pontos menores (unicidade de `calendar_holidays` via sentinela mágico; ausência de nota sobre Gates 03/04/08/09 ficarem `BLOQUEADO` por desenho). 2ª rodada (agente independente, verificação achado a achado contra o código real, citações reverificadas pessoalmente antes de aceitar) — `NO-GO` inicial com 6 achados: (1) **crítico** — RLS de 7 objetos novos usava `is_member` (checagem puramente org-wide), vazando leitura entre unidades da mesma organização; deveria usar `private.can_access_fact_unit`, o padrão já estabelecido pelo addendum de segurança da Onda 0 para exatamente esta classe de tabela (`appointments`/`orders`/`payments`/`units`); (2) **crítico** — timezone da unidade (Master §2.4, CRÍTICO) nunca mencionado, sem mecanismo descrito para converter os blocos `HH:MM` locais em `timestamptz`; (3) `resource_lock_create`/`resource_lock_release` referenciados em prosa sem contrato algum, e a concorrência otimista de `resource_locks` descrevia só o incremento de `version`, sem o passo de comparação/rejeição que a torna um lock de fato (mesma lacuna que a ADR 0012 diagnosticou e corrigiu para `appointments.version`); (4) FK de `appointment_id` em `calendar_policy_conflicts`/`resource_locks` sem amarrar `unit_id` ao `unit_id` real do appointment — mesmo gap que o red team da Onda 3 já achou e corrigiu para `commission_sale_records.order_id`; (5) trigger de vigência sem validação de ordem cronológica, e analogia tecnicamente incorreta com os triggers de default-fill da Onda 0 (que só escrevem na própria linha, nunca em linha irmã); (6) regra CRÍTICO "turno ⊆ horário da unidade" sem materialização nem registro como risco aceito. Todos os 6 corrigidos; 2ª rodada `GO` após correção. Etapa 8 local foi autorizada pelo Platform Owner na mesma sessão e concluída nas fatias `023`-`028`; Red Team de implementação e auditoria final de fix corrigiram os gaps reais conhecidos, com evidência final 764/764 pgTAP, 321/321 backend e `supabase db lint --local` sem erros. Ver [ADR 0022](../adr/0022-onda4-calendar-policy-availability-resource-orchestration.md) para o detalhamento completo, as alternativas rejeitadas e o texto integral dos achados corrigidos. | Regulariza neste registro a aprovação do Blueprint e a Etapa 8 local já executada na mesma sessão. Não autoriza promoção para `staging`, `main` ou produção; esses passos seguem dependentes dos gates de ambiente, entrega e homologação. Não reabre DEC-24/27/28/44. |

---

# SEÇÃO 16 — DEC-50: HARDENING DOS AGENTES CUSTOMIZADOS `KORTEX-*` (2026-08-04)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-50 | Execução completa do `SPEC_HARDENING_AGENTES_KORTEX.md` (tooling/governança de agentes, não fatia de produto — não segue o funil Truth Map → Migration Map → Blueprint) | **Executado com aprovação do Platform Owner em 2026-08-04**, a partir de auditoria prévia (`PROPOSTA_HARDENING_AGENTES_KORTEX.md`) que achou 5 problemas nos 10 agentes `.agents/skills/kortex-*`: (A) links `file:///c:/...` não-portáveis em 10/10 agentes; (B) guardrail de "reverificar antes de confiar em relato de subagente" presente em só 3/10; (C) `kortex-express-architect` sem seção "8. Condição de parada" corretamente grafada (tinha "Condition de parada", cabeçalho quebrado, e `description` genérico); (D) risco de os agentes não serem descobertos nativamente pelo Claude Code (`.agents/skills/` vs. o caminho de descoberta documentado `.claude/skills/`); (E) vendor skills de UI (`ui-ux-pro-max`, `frontend-design`) sem noção do Design System real do projeto. **Pré-condição bloqueante testada antes de qualquer edição:** a sessão foi perguntada diretamente quais skills `kortex-*` enxergava — nenhum dos 10 apareceu, confirmando o Problema D como real, não hipotético. **Fatias 1/3/2/6 (PR #28, branch `chore/agent-hardening` a partir de `staging`):** Fatia 1 substituiu os links absolutos por caminhos relativos nos 10 `SKILL.md` (`grep -r "file:///"` vazio, confirmado); Fatia 3 corrigiu a seção 8 do `kortex-express-architect` e expandiu seu `description` com gatilhos específicos (rotas HTTP, middlewares, controllers, services); Fatia 2 adicionou a cláusula de reverificação — mecanismo concreto, não lembrete genérico — em `kortex-mvpt-orchestrator`, `kortex-environment-guardian`, `kortex-delivery-guardian` e `kortex-migration-mapper`, uma cláusula aprovada por vez pelo Platform Owner; Fatia 6 subordinou `ui-ux-pro-max`/`frontend-design` ao Design System real (`frontend/src/ui/primitives/`, `frontend/src/ui/foundations/tokens.css`) via cláusula só em `kortex-pwa-architect`, sem editar nenhum vendor skill (confirmado por `git status --short`); Fatia 4 copiou os 10 diretórios `kortex-*` para `.claude/skills/` (cópia real, não symlink — `diff -rq` idêntico) e registrou em `AGENTS.md` a regra de replicação obrigatória no mesmo turno; o efeito foi confirmado empiricamente (a mesma sessão passou a listar os 10 `kortex-*` depois da cópia). **Fatia 7 (PRs #29-#34, uma branch e um PR por sub-etapa a partir de `staging`, merge só após teste manual caso-ruim/caso-bom):** implementados 5 hooks determinísticos em `.claude/hooks/` + `.claude/settings.json` — 7.1 `check-secret-commit.js` (`PreToolUse`/`Bash`, bloqueia `git commit` com padrão de segredo); 7.2 `check-branch-protection.js` (`PreToolUse`/`Bash`, bloqueia commit/push direto em `main`/`staging`); 7.3 `check-tenant-invariant.js` (`PostToolUse`/`Edit`|`Write`|`MultiEdit`, sinaliza `req.body.tenant_id`/`req.query.tenant_id` em `*.route.js`); 7.4 `check-design-system.js` (mesmo matcher, sinaliza `<button>`/`<input>` nativos fora de `ui/primitives`); 7.5 `check-blueprint-gate.js` (`PreToolUse`/`Write`|`Edit`|`MultiEdit`, bloqueia `supabase/migrations/*.sql` sem referência real a um Blueprint existente). **Duas decisões que o spec não especificava e exigiram escolha explícita do Platform Owner, em vez de invenção unilateral:** (i) Fatia 7.3 mantida no escopo literal do spec (`tenant_id`) mesmo sabendo que o campo real de tenant desta base é `organization_id` — gap documentado no PR #31, não corrigido nesta rodada; (ii) Fatia 7.5, o sinal determinístico escolhido foi "cabeçalho/comentário na própria migration referenciando o Blueprint" (não branch, não existência de issue), e o Platform Owner confirmou explicitamente que o efeito colateral encontrado no teste — 32 das 45 migrations existentes hoje não têm essa referência e ficam bloqueadas para edição até ganharem a linha — é o comportamento desejado (PR #33). **Um bug real pego pelo próprio teste manual antes do commit:** a regex do hook 7.4 tinha a flag `/i`, então `<Button>` (componente PascalCase do Design System) também batia com `button` e gerava falso positivo no caso bom — corrigido removendo a flag antes de commitar (PR #32). **Um gap real só encontrado em validação AO VIVO pedida explicitamente pelo Platform Owner após a Fatia 7 estar mesclada** ("valida os hooks numa sessão nova antes de mesclar mais alguma coisa"): o hook 7.1 só checava `git diff --cached`, que fica vazio quando criação do arquivo + `git add` + `git commit` estão numa única chamada Bash encadeada (o `PreToolUse` dispara antes do comando rodar) — reproduzido com um commit real (chave AWS fake, revertido localmente, nunca pushado) que passou sem ser bloqueado. Corrigido (PR #34) escaneando também o texto bruto do comando, testado ao vivo nos 3 casos (segredo no texto do comando → bloqueado; segredo já staged por chamada separada → ainda bloqueado, sem regressão; sem segredo → passa). **Validação ao vivo final dos 5 hooks, harness real (não só invocação manual dos scripts):** 7.2 bloqueou um `git commit` real em `staging`; 7.5 bloqueou um `Write` real de migration sem referência antes do arquivo existir; 7.3 e 7.4 sinalizaram, respectivamente, um `Write` real com `req.body.tenant_id` e com `<button>` nativo; 7.1, após o fix, passou nos 3 casos ao vivo. **Fatia 7 tem gate de entrada e ordem própria e foi respeitada:** não iniciada antes de Fatias 1/2/3/6 mescladas em `staging`; 5 sub-etapas executadas em ordem (7.1→7.5), uma branch e um PR por sub-etapa, merge só após teste manual — nenhuma paralelizada. | Fecha a execução do `SPEC_HARDENING_AGENTES_KORTEX.md` (Fatias 1, 2, 3, 4, 6 e 7, incluindo o fix de 7.1). **Não fecha:** a Fatia 5 não existe no spec (numeração pula de 4 para 6, sem Fatia 5 definida — nada a fazer); o gap `organization_id` da Fatia 7.3 permanece aberto, exigindo PR próprio se o Platform Owner quiser cobertura real; a Fatia 7 (hooks) já está em execução — o spec original a descrevia como bloqueada por um gate de entrada, satisfeito nesta mesma sessão após validação em staging. Não reabre nenhuma decisão de produto (DEC-04 a DEC-49) nem o modelo de arquitetura de agentes. |

---

# SEÇÃO 17 — DEC-51: APROVAÇÃO DO BLUEPRINT DA ONDA 5 — RECURRING, GROUP BOOKING & WAITLIST (2026-08-04)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-51 | Aprovação do Blueprint da Onda 5 (Recurring, Group Booking & Waitlist) | Aprovado pelo Platform Owner em 2026-08-04. Blueprint da Onda 5 aprovado com recorrência de 8 semanas, anchor_date/local_start_time, timezone da unidade, materialização parcial com conflitos persistidos, Group Booking pai/filhos 2–10 com profissionais diferentes e waitlist Booksy sem hold persistente antes do clique, usando token, TTL de 30 minutos, cooldown de 6 horas, CAS, idempotência e SUPERSEDED. Red Team de desenho corrigiu os gaps de create_appointment, Availability Resolver, âncora temporal, FKs unit-safe, titular/backfill, token/offer_id, deduplicação e concorrência. Gate final: GO. Etapa 8 permanece bloqueada; nenhum SQL, migration, código, UI ou deploy é autorizado por este DEC. Ver ADR 0023 e Blueprint Onda 5. | Não revoga o Migration Map nem o Master Briefing; refina o hold da waitlist no escopo transacional da confirmação e materializa Group Booking com objetos de apoio dentro do mesmo domínio. |

---

# SEÇÃO 18 — DEC-52: AUTORIZAÇÃO DA ETAPA 8 LOCAL DA ONDA 5 (2026-08-04)

| # | Decisão | Conteúdo | Substitui |
|---:|---|---|---|
| DEC-52 | Autorização da Etapa 8 (SQL) da Onda 5 para ambiente local | **Aprovado pelo Platform Owner em 2026-08-04, decorrência de DEC-51.** Autoriza implementar e testar, uma fatia por vez, as issues 029–035 da Onda 5 em ambiente Supabase local/descartável e branch de trabalho, usando TDD (pgTAP e node --test), pre-flight por migration, feature flag desligada por padrão e Red Team de implementação antes de declarar a Onda concluída. A autorização cobre migrations novas, RPCs, backend e UI estritamente necessários ao Blueprint/ADR 0023; não autoriza editar migrations aplicadas, ativar a feature flag para clientes, promover para staging/main, fazer deploy ou alterar produção. | Desbloqueia somente a implementação local da Onda 5. Promoção continua dependente de Environment Guardian, Delivery Guardian, CI e homologação. |
